import datetime
import depthai
import depthai.beta
import depthai.nn_archive.v1
import depthai.node
from _typeshed import Incomplete
from typing import ClassVar, overload

class ClassificationParser(depthai.DeviceNode):
    """ClassificationParser node. Parses the raw output of a classification neural
    network into a dai::beta::Classifications message with class names and scores
    sorted in descending order of score.

    The parser consumes a single output tensor. When the incoming NNData contains
    exactly one tensor, it is selected automatically; otherwise the output layer
    name must be configured explicitly or through an NNArchive head. Raw scores are
    dequantized and flattened; when the model output is not already softmaxed, the
    parser applies softmax to convert the scores to probabilities."""
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> ClassificationParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.ClassificationParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.ClassificationParser

        Build ClassificationParser node. Links the supplied output to this node's input
        and configures the parser from the model's classification head.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.ClassificationParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.ClassificationParser

        Build ClassificationParser node with the specific head from an NNArchive. Useful
        when the model has multiple classification heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser
        """
    def getClasses(self) -> list[str]:
        """getClasses(self: depthai.beta.node.ClassificationParser) -> list[str]

        Returns the class names to link with the classification scores.
        """
    def getOutputLayerName(self) -> str:
        """getOutputLayerName(self: depthai.beta.node.ClassificationParser) -> str

        Returns the name of the model output layer to parse.
        """
    def getSoftmax(self) -> bool:
        """getSoftmax(self: depthai.beta.node.ClassificationParser) -> bool

        Returns whether the model output is treated as already softmaxed.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.ClassificationParser) -> bool

        Returns true when this node runs on the host.

        Host-only pipelines always run the node on the host.
        """
    def setClasses(self, classes: list[str]) -> None:
        """setClasses(self: depthai.beta.node.ClassificationParser, classes: list[str]) -> None

        Sets the class names to link with the classification scores.

        The class names are expected to be in the same order as the neural network's
        output. The number of class names must match the number of scores produced by
        the model.

        Parameter ``classes``:
            Vector of class names
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.ClassificationParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one
        ClassificationParser head; use setNNArchiveHead() to select a specific head from
        a multi-head archive.

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        """setNNArchiveHead(self: depthai.beta.node.ClassificationParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head must be a ClassificationParser head
        with exactly one output layer.

        Parameter ``head:``:
            NNArchive head to set
        """
    def setOutputLayerName(self, outputLayerName: str) -> None:
        """setOutputLayerName(self: depthai.beta.node.ClassificationParser, outputLayerName: str) -> None

        Sets the name of the model output layer to parse.

        When left empty, the parser selects the tensor automatically if the incoming
        NNData contains exactly one tensor and fails otherwise.

        Parameter ``outputLayerName``:
            Name of the output layer
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.ClassificationParser, runOnHost: bool) -> None

        Select whether the node runs on the host or device.
        """
    def setSoftmax(self, isSoftmax: bool) -> None:
        """setSoftmax(self: depthai.beta.node.ClassificationParser, isSoftmax: bool) -> None

        Sets whether the model output is already softmaxed.

        When false, the parser applies softmax to convert the raw scores to
        probabilities.

        Parameter ``isSoftmax``:
            True when the model output is already softmaxed
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with classification data to parse.
        (self: depthai.beta.node.ClassificationParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs Classifications message with classes and scores sorted in descending
        order of score.
        (self: depthai.beta.node.ClassificationParser) -> depthai.Node.Output
        """

class ClassificationSequenceParser(depthai.DeviceNode):
    """ClassificationSequenceParser node. Parses the raw output of a classification
    sequence neural network into a dai::beta::Classifications message with class
    names and scores ordered by their position in the sequence.

    The model predicts the classes multiple times and returns a list of predicted
    classes, where each item corresponds to the relative step in the sequence. In
    addition to time series classification, this parser can also be used for text
    recognition models where words can be interpreted as a sequence of characters
    (classes).

    The parser consumes a single output tensor of shape (sequenceLength, nClasses),
    (1, sequenceLength, nClasses) or (sequenceLength, nClasses, 1). When the
    incoming NNData contains exactly one tensor, it is selected automatically;
    otherwise the output layer name must be configured explicitly or through an
    NNArchive head. Raw scores are dequantized; when the model output is not already
    softmaxed, the parser applies softmax along each sequence step to convert the
    scores to probabilities."""
    Properties: ClassVar[type[depthai.beta.ClassificationSequenceParserProperties]] = ...
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    @overload
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> ClassificationSequenceParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.ClassificationSequenceParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.ClassificationSequenceParser

        Build ClassificationSequenceParser node. Links the supplied output to this
        node's input and configures the parser from the model's classification sequence
        head.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.ClassificationSequenceParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.ClassificationSequenceParser

        Build ClassificationSequenceParser node with the specific head from an
        NNArchive. Useful when the model has multiple classification sequence heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser
        """
    @overload
    def build(self, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> ClassificationSequenceParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.ClassificationSequenceParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.ClassificationSequenceParser

        Build ClassificationSequenceParser node. Links the supplied output to this
        node's input and configures the parser from the model's classification sequence
        head.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.ClassificationSequenceParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.ClassificationSequenceParser

        Build ClassificationSequenceParser node with the specific head from an
        NNArchive. Useful when the model has multiple classification sequence heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser
        """
    def getClasses(self) -> list[str]:
        """getClasses(self: depthai.beta.node.ClassificationSequenceParser) -> list[str]

        Returns the class names to link with the per-step classification scores.
        """
    def getConcatenateClasses(self) -> bool:
        """getConcatenateClasses(self: depthai.beta.node.ClassificationSequenceParser) -> bool

        Returns whether the remaining classes are concatenated.
        """
    def getIgnoredIndexes(self) -> list[int]:
        """getIgnoredIndexes(self: depthai.beta.node.ClassificationSequenceParser) -> list[int]

        Returns the class indexes ignored during classification sequence generation.
        """
    def getOutputLayerName(self) -> str:
        """getOutputLayerName(self: depthai.beta.node.ClassificationSequenceParser) -> str

        Returns the name of the model output layer to parse.
        """
    def getRemoveDuplicates(self) -> bool:
        """getRemoveDuplicates(self: depthai.beta.node.ClassificationSequenceParser) -> bool

        Returns whether consecutive duplicate classes are removed from the sequence.
        """
    def getSoftmax(self) -> bool:
        """getSoftmax(self: depthai.beta.node.ClassificationSequenceParser) -> bool

        Returns whether the model output is treated as already softmaxed.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.ClassificationSequenceParser) -> bool

        Returns true when this node runs on the host.

        Host-only pipelines always run the node on the host.
        """
    def setClasses(self, classes: list[str]) -> None:
        """setClasses(self: depthai.beta.node.ClassificationSequenceParser, classes: list[str]) -> None

        Sets the class names to link with the per-step classification scores.

        The class names are expected to be in the same order as the neural network's
        output. The number of class names must match the number of scores produced by
        the model at each sequence step.

        Parameter ``classes``:
            Vector of class names
        """
    def setConcatenateClasses(self, concatenateClasses: bool) -> None:
        '''setConcatenateClasses(self: depthai.beta.node.ClassificationSequenceParser, concatenateClasses: bool) -> None

        Sets whether the remaining classes are concatenated. Used mostly for text
        processing.

        When true and more than one class remains: when all remaining class names are at
        most one character long, they are joined and split on whitespace into words with
        a per-word mean score; otherwise all class names are joined into a single string
        with a " " separator and one mean score.

        Parameter ``concatenateClasses``:
            True to concatenate the remaining classes @note Configures startup behavior.
            Send ClassificationSequenceParserConfig to inputConfig after the pipeline
            starts.
        '''
    def setIgnoredIndexes(self, ignoredIndexes: list[int]) -> None:
        """setIgnoredIndexes(self: depthai.beta.node.ClassificationSequenceParser, ignoredIndexes: list[int]) -> None

        Sets the class indexes to ignore during classification sequence generation (e.g.
        background class, blank space).

        Sequence steps whose most probable class index is listed here are dropped from
        the output. Every index must be within [0, nClasses - 1].

        Parameter ``ignoredIndexes``:
            Vector of class indexes to ignore @note Configures startup behavior. Send
            ClassificationSequenceParserConfig to inputConfig after the pipeline starts.
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.ClassificationSequenceParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one
        ClassificationSequenceParser head; use setNNArchiveHead() to select a specific
        head from a multi-head archive.

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        """setNNArchiveHead(self: depthai.beta.node.ClassificationSequenceParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head must be a
        ClassificationSequenceParser head with exactly one output layer.

        Parameter ``head:``:
            NNArchive head to set
        """
    def setOutputLayerName(self, outputLayerName: str) -> None:
        """setOutputLayerName(self: depthai.beta.node.ClassificationSequenceParser, outputLayerName: str) -> None

        Sets the name of the model output layer to parse.

        When left empty, the parser selects the tensor automatically if the incoming
        NNData contains exactly one tensor and fails otherwise.

        Parameter ``outputLayerName``:
            Name of the output layer
        """
    def setRemoveDuplicates(self, removeDuplicates: bool) -> None:
        """setRemoveDuplicates(self: depthai.beta.node.ClassificationSequenceParser, removeDuplicates: bool) -> None

        Sets whether consecutive duplicate classes are removed from the sequence.

        Only consecutive duplicates are removed; repeated classes separated by other
        classes are kept.

        Parameter ``removeDuplicates``:
            True to remove consecutive duplicates from the sequence @note Configures
            startup behavior. Send ClassificationSequenceParserConfig to inputConfig
            after the pipeline starts.
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.ClassificationSequenceParser, runOnHost: bool) -> None

        Select whether the node runs on the host or device.
        """
    def setSoftmax(self, isSoftmax: bool) -> None:
        """setSoftmax(self: depthai.beta.node.ClassificationSequenceParser, isSoftmax: bool) -> None

        Sets whether the model output is already softmaxed.

        When false, the parser applies softmax along each sequence step to convert the
        raw scores to probabilities.

        Parameter ``isSoftmax``:
            True when the model output is already softmaxed
        """
    @property
    def initialConfig(self) -> depthai.beta.ClassificationSequenceParserConfig:
        """Configuration used until a message is received on inputConfig.
        (self: depthai.beta.node.ClassificationSequenceParser) -> depthai.beta.ClassificationSequenceParserConfig
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with classification sequence data to parse.
        (self: depthai.beta.node.ClassificationSequenceParser) -> depthai.Node.Input
        """
    @property
    def inputConfig(self) -> depthai.Node.Input:
        """Runtime parser configuration. When synchronized, one configuration is consumed
        per frame; otherwise all queued configurations are drained and the newest valid
        one is used.
        (self: depthai.beta.node.ClassificationSequenceParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs Classifications message with classes and scores ordered by their
        position in the sequence.
        (self: depthai.beta.node.ClassificationSequenceParser) -> depthai.Node.Output
        """

class EmbeddingsParser(depthai.DeviceNode):
    """EmbeddingsParser node. Validates the raw output of an embeddings neural network
    model head and forwards it unchanged as a dai::NNData message.

    The parser expects a single output tensor carrying the embedding vector. When
    the output layer name is left unconfigured, every incoming NNData must contain
    exactly one tensor; otherwise the message is rejected. The message itself is
    forwarded without modification, so all tensors, sequence number, timestamps, and
    image transformation metadata are preserved."""
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> EmbeddingsParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.EmbeddingsParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.EmbeddingsParser

        Build EmbeddingsParser node. Links the supplied output to this node's input and
        configures the parser from the model's embeddings head.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.EmbeddingsParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.EmbeddingsParser

        Build EmbeddingsParser node with the specific head from an NNArchive. Useful
        when the model has multiple embeddings heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser
        """
    def getOutputLayerName(self) -> str:
        """getOutputLayerName(self: depthai.beta.node.EmbeddingsParser) -> str

        Returns the name of the model output layer carrying the embeddings.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.EmbeddingsParser) -> bool

        Returns true when this node runs on the host.

        Host-only pipelines always run the node on the host.
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.EmbeddingsParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one
        EmbeddingsParser head; use setNNArchiveHead() to select a specific head from a
        multi-head archive.

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        """setNNArchiveHead(self: depthai.beta.node.EmbeddingsParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head must be an EmbeddingsParser head with
        exactly one output layer.

        Parameter ``head:``:
            NNArchive head to set
        """
    def setOutputLayerName(self, outputLayerName: str) -> None:
        """setOutputLayerName(self: depthai.beta.node.EmbeddingsParser, outputLayerName: str) -> None

        Sets the name of the model output layer carrying the embeddings.

        When left empty, the parser requires the incoming NNData to contain exactly one
        tensor and fails otherwise.

        Parameter ``outputLayerName``:
            Name of the output layer
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.EmbeddingsParser, runOnHost: bool) -> None

        Select whether the node runs on the host or device.
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with embeddings data to validate and forward.
        (self: depthai.beta.node.EmbeddingsParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs the unchanged NNData message containing the embeddings output layer.
        (self: depthai.beta.node.EmbeddingsParser) -> depthai.Node.Output
        """

class FastSAMParser(depthai.DeviceNode):
    '''FastSAMParser node. Parses the output of the FastSAM segmentation model
    (https://github.com/CASIA-IVA-Lab/FastSAM) into a dai::SegmentationMask message
    where each pixel holds the index of the instance it belongs to and 255 marks
    background.

    The parser consumes the model\'s YOLO detection outputs (NCHW tensors of shape
    (1, numClasses + 5, gridH, gridW), sorted by layer name and decoded anchorless
    with strides 8/16/32), the per-head mask-coefficient outputs (NCHW tensors of
    shape (1, numPrototypes, gridH, gridW), sorted by layer name) and the prototype
    masks output (NCHW tensor of shape (1, numPrototypes, protoH, protoW)). The
    model input size is derived from the first (stride-8) YOLO output\'s grid times
    8; the number of prototypes from the protos tensor\'s channel count. Boxes pass
    confidence filtering and non-maximum suppression, boxes within 20 pixels of the
    image border are snapped to it, and a box overlapping the full image with IoU >
    0.9 is replaced by the full-image box. Each kept detection\'s mask is combined
    from the prototypes, resized to the model input size with nearest-neighbor
    interpolation, cropped to its box and binarized with the mask confidence
    threshold.

    The prompt selects the emitted instances: "everything" keeps all detections
    (later, lower-confidence instances overwrite earlier ones on overlapping
    pixels), "bbox" keeps the single mask with the highest IoU against the prompt
    bounding box, and "point" combines the masks containing the prompt point (added
    for point label 1, subtracted for 0). With no detections, a fully-background
    mask is emitted.'''
    Properties: ClassVar[type[depthai.beta.FastSAMParserProperties]] = ...
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> FastSAMParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.FastSAMParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.FastSAMParser

        Build FastSAMParser node. Links the supplied output to this node's input and
        configures the parser from the model's FastSAMParser head.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.FastSAMParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.FastSAMParser

        Build FastSAMParser node with the specific head from an NNArchive. Useful when
        the model has multiple heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser
        """
    def getBoundingBox(self, *args, **kwargs):
        """getBoundingBox(self: depthai.beta.node.FastSAMParser) -> Optional[Annotated[list[int], FixedSize(4)]]

        Returns the prompt bounding box as (x1, y1, x2, y2), or std::nullopt when it is
        not set.
        """
    def getConfidenceThreshold(self) -> float:
        """getConfidenceThreshold(self: depthai.beta.node.FastSAMParser) -> float

        Returns the confidence score threshold for detected objects.
        """
    def getIouThreshold(self) -> float:
        """getIouThreshold(self: depthai.beta.node.FastSAMParser) -> float

        Returns the non-maximum suppression overlap threshold.
        """
    def getMaskConfidence(self) -> float:
        """getMaskConfidence(self: depthai.beta.node.FastSAMParser) -> float

        Returns the mask confidence threshold.
        """
    def getMaskOutputs(self) -> list[str]:
        """getMaskOutputs(self: depthai.beta.node.FastSAMParser) -> list[str]

        Returns the names of the model's mask-coefficient output layers.
        """
    def getNumClasses(self) -> int:
        """getNumClasses(self: depthai.beta.node.FastSAMParser) -> int

        Returns the number of classes in the model.
        """
    def getPointLabel(self) -> int | None:
        """getPointLabel(self: depthai.beta.node.FastSAMParser) -> Optional[int]

        Returns the prompt point label, or std::nullopt when it is not set.
        """
    def getPoints(self) -> tuple[int, int] | None:
        """getPoints(self: depthai.beta.node.FastSAMParser) -> Optional[tuple[int, int]]

        Returns the prompt point as (x, y), or std::nullopt when it is not set.
        """
    def getPrompt(self) -> str:
        """getPrompt(self: depthai.beta.node.FastSAMParser) -> str

        Returns the prompt type.
        """
    def getProtosOutput(self) -> str:
        """getProtosOutput(self: depthai.beta.node.FastSAMParser) -> str

        Returns the name of the model's prototype-masks output layer.
        """
    def getYoloOutputs(self) -> list[str]:
        """getYoloOutputs(self: depthai.beta.node.FastSAMParser) -> list[str]

        Returns the names of the model's YOLO output layers.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.FastSAMParser) -> bool

        Returns true when this node runs on the host.

        Host-only pipelines always run the node on the host.
        """
    def setBoundingBox(self, bbox) -> None:
        '''setBoundingBox(self: depthai.beta.node.FastSAMParser, bbox: Annotated[list[int], FixedSize(4)]) -> None

        Sets the prompt bounding box as (x1, y1, x2, y2) in model-input pixels, used by
        the "bbox" prompt. Unset by default; the "bbox" prompt requires it and its x2
        and y2 coordinates must not be 0.

        Parameter ``bbox``:
            Bounding box as (x1, y1, x2, y2) @note Configures startup behavior. Send
            FastSAMParserConfig to inputConfig after the pipeline starts.
        '''
    def setConfidenceThreshold(self, threshold: float) -> None:
        """setConfidenceThreshold(self: depthai.beta.node.FastSAMParser, threshold: float) -> None

        Sets the confidence score threshold for detected objects. Detections whose score
        is strictly greater than the threshold are kept. Defaults to 0.5.

        Parameter ``threshold``:
            Confidence score threshold, must be between 0 and 1 @note Configures startup
            behavior. Send FastSAMParserConfig to inputConfig after the pipeline starts.
        """
    def setIouThreshold(self, iouThreshold: float) -> None:
        """setIouThreshold(self: depthai.beta.node.FastSAMParser, iouThreshold: float) -> None

        Sets the non-maximum suppression overlap threshold. Boxes whose overlap with a
        kept box is strictly greater than the threshold are suppressed. Defaults to 0.5.

        Parameter ``iouThreshold``:
            Overlap threshold, must be between 0 and 1 @note Configures startup
            behavior. Send FastSAMParserConfig to inputConfig after the pipeline starts.
        """
    def setMaskConfidence(self, maskConfidence: float) -> None:
        """setMaskConfidence(self: depthai.beta.node.FastSAMParser, maskConfidence: float) -> None

        Sets the mask confidence threshold used to binarize instance masks. Mask pixels
        with a sigmoid probability strictly greater than the threshold belong to the
        instance. Defaults to 0.5.

        Parameter ``maskConfidence``:
            Mask confidence threshold, must be between 0 and 1 @note Configures startup
            behavior. Send FastSAMParserConfig to inputConfig after the pipeline starts.
        """
    def setMaskOutputs(self, maskOutputs: list[str]) -> None:
        '''setMaskOutputs(self: depthai.beta.node.FastSAMParser, maskOutputs: list[str]) -> None

        Sets the names of the model\'s mask-coefficient output layers. Only names
        containing "mask" are used, sorted by name and index-aligned with the sorted
        YOLO output layers; when empty, all layer names of the incoming NNData
        containing "mask" are used. Defaults to ["output1_masks", "output2_masks",
        "output3_masks"].

        Parameter ``maskOutputs``:
            Names of the mask output layers
        '''
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.FastSAMParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one FastSAMParser
        head; use setNNArchiveHead() to select a specific head from a multi-head
        archive.

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        '''setNNArchiveHead(self: depthai.beta.node.FastSAMParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head\'s output layer names containing
        "_yolo" configure the YOLO output layers and those containing "_masks" the mask
        output layers (each only when at least one matches). The confidence threshold,
        number of classes, NMS threshold, mask confidence, prompt, points, point label
        and bounding box are read from the head metadata when present.

        Parameter ``head:``:
            NNArchive head to set
        '''
    def setNumClasses(self, numClasses: int) -> None:
        """setNumClasses(self: depthai.beta.node.FastSAMParser, numClasses: int) -> None

        Sets the number of classes in the model. The YOLO output tensors must have
        numClasses + 5 channels. Defaults to 1.

        Parameter ``numClasses``:
            Number of classes, must be greater than 0
        """
    def setPointLabel(self, pointLabel: int) -> None:
        '''setPointLabel(self: depthai.beta.node.FastSAMParser, pointLabel: int) -> None

        Sets the prompt point label, used by the "point" prompt: 1 adds the instance
        masks containing the point, 0 subtracts them. Unset by default; the "point"
        prompt requires it.

        Parameter ``pointLabel``:
            Point label @note Configures startup behavior. Send FastSAMParserConfig to
            inputConfig after the pipeline starts.
        '''
    def setPoints(self, x: int, y: int) -> None:
        '''setPoints(self: depthai.beta.node.FastSAMParser, x: int, y: int) -> None

        Sets the prompt point as (x, y) in model-input pixels, used by the "point"
        prompt. Unset by default; the "point" prompt requires it.

        Parameter ``x``:
            Point x coordinate

        Parameter ``y``:
            Point y coordinate @note Configures startup behavior. Send
            FastSAMParserConfig to inputConfig after the pipeline starts.
        '''
    def setPrompt(self, prompt: str) -> None:
        '''setPrompt(self: depthai.beta.node.FastSAMParser, prompt: str) -> None

        Sets the prompt type: "everything" emits every detected instance, "bbox" the
        single instance mask with the highest IoU against the prompt bounding box (see
        setBoundingBox()), and "point" the combination of the instance masks containing
        the prompt point (see setPoints() and setPointLabel()). Defaults to
        "everything".

        Parameter ``prompt``:
            Prompt type, one of "everything", "bbox" or "point" @note Configures startup
            behavior. Send FastSAMParserConfig to inputConfig after the pipeline starts.
        '''
    def setProtosOutput(self, protosOutput: str) -> None:
        '''setProtosOutput(self: depthai.beta.node.FastSAMParser, protosOutput: str) -> None

        Sets the name of the model\'s prototype-masks output layer; when empty,
        "protos_output" is used. Defaults to "protos_output".

        Parameter ``protosOutput``:
            Name of the protos output layer
        '''
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.FastSAMParser, runOnHost: bool) -> None

        Select whether the node runs on the host or device.
        """
    def setYoloOutputs(self, yoloOutputs: list[str]) -> None:
        '''setYoloOutputs(self: depthai.beta.node.FastSAMParser, yoloOutputs: list[str]) -> None

        Sets the names of the model\'s YOLO output layers. The layers are processed
        sorted by name, so the stride-8 head must come first in sort order. Defaults to
        ["output1_yolov8", "output2_yolov8", "output3_yolov8"].

        Parameter ``yoloOutputs``:
            Names of the YOLO output layers
        '''
    @property
    def initialConfig(self) -> depthai.beta.FastSAMParserConfig:
        """Configuration used until a message is received on inputConfig.
        (self: depthai.beta.node.FastSAMParser) -> depthai.beta.FastSAMParserConfig
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with FastSAM data to parse.
        (self: depthai.beta.node.FastSAMParser) -> depthai.Node.Input
        """
    @property
    def inputConfig(self) -> depthai.Node.Input:
        """Runtime parser configuration. When synchronized, one configuration is consumed
        per frame; otherwise all queued configurations are drained and the newest valid
        one is used.
        (self: depthai.beta.node.FastSAMParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs SegmentationMask message with the resulting segmentation masks given the
        prompt.
        (self: depthai.beta.node.FastSAMParser) -> depthai.Node.Output
        """

class HRNetParser(depthai.DeviceNode):
    """HRNetParser node. Parses the heatmap output of an HRNet pose estimation neural
    network into a dai::beta::Keypoints message. The decoding is inspired by
    https://github.com/ibaiGorordo/ONNX-HRNET-Human-Pose-Estimation.

    The parser consumes a single output tensor. When the incoming NNData contains
    exactly one tensor, it is selected automatically; otherwise the output layer
    name must be configured explicitly or through an NNArchive head. The tensor is
    read in NCHW orientation regardless of its stored order; after squeezing a
    leading batch dimension of 1 it must be a 3D tensor of shape (numKeypoints,
    height, width). The number of keypoints and the heatmap size are derived from
    the tensor shape. Per heatmap, the keypoint is the position of the maximum value
    normalized by the heatmap size and the keypoint's score is the maximum value
    clipped to [0, 1]. Keypoints with a score below the score threshold are dropped
    and the skeleton edges are remapped to the kept keypoints."""
    Properties: ClassVar[type[depthai.beta.HRNetParserProperties]] = ...
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> HRNetParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.HRNetParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.HRNetParser

        Build HRNetParser node. Links the supplied output to this node's input and
        configures the parser from the model's HRNetParser head.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.HRNetParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.HRNetParser

        Build HRNetParser node with the specific head from an NNArchive. Useful when the
        model has multiple heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser
        """
    def getEdges(self, *args, **kwargs):
        """getEdges(self: depthai.beta.node.HRNetParser) -> list[Annotated[list[int], FixedSize(2)]]

        Returns the skeleton edges as pairs of keypoint indices.
        """
    def getLabelNames(self) -> list[str]:
        """getLabelNames(self: depthai.beta.node.HRNetParser) -> list[str]

        Returns the label names for the keypoints.
        """
    def getOutputLayerName(self) -> str:
        """getOutputLayerName(self: depthai.beta.node.HRNetParser) -> str

        Returns the name of the model output layer to parse.
        """
    def getScoreThreshold(self) -> float:
        """getScoreThreshold(self: depthai.beta.node.HRNetParser) -> float

        Returns the confidence score threshold for detected keypoints.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.HRNetParser) -> bool

        Returns true when this node runs on the host.

        Host-only pipelines always run the node on the host.
        """
    def setEdges(self, edges) -> None:
        """setEdges(self: depthai.beta.node.HRNetParser, edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        Sets the skeleton edges as pairs of keypoint indices used for visualizing the
        skeleton.

        Example: {{0, 1}, {1, 2}, {2, 3}, {3, 0}} connects keypoint 0 to keypoint 1,
        keypoint 1 to keypoint 2, etc.

        Parameter ``edges``:
            Vector of keypoint index pairs
        """
    def setLabelNames(self, labelNames: list[str]) -> None:
        """setLabelNames(self: depthai.beta.node.HRNetParser, labelNames: list[str]) -> None

        Sets the label names for the keypoints, indexed by keypoint index.

        Parameter ``labelNames``:
            Vector of label names
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.HRNetParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one HRNetParser
        head; use setNNArchiveHead() to select a specific head from a multi-head
        archive.

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        """setNNArchiveHead(self: depthai.beta.node.HRNetParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head must be an HRNetParser head with
        exactly one output layer.

        Parameter ``head:``:
            NNArchive head to set
        """
    def setOutputLayerName(self, outputLayerName: str) -> None:
        """setOutputLayerName(self: depthai.beta.node.HRNetParser, outputLayerName: str) -> None

        Sets the name of the model output layer to parse.

        When left empty, the parser selects the tensor automatically if the incoming
        NNData contains exactly one tensor and fails otherwise.

        Parameter ``outputLayerName``:
            Name of the output layer
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.HRNetParser, runOnHost: bool) -> None

        Select whether the node runs on the host or device.
        """
    def setScoreThreshold(self, threshold: float) -> None:
        """setScoreThreshold(self: depthai.beta.node.HRNetParser, threshold: float) -> None

        Sets the confidence score threshold for detected keypoints. Keypoints with a
        score strictly below the threshold are dropped.

        Parameter ``threshold``:
            Confidence score threshold, must be between 0 and 1 @note Configures startup
            behavior. Send HRNetParserConfig to inputConfig after the pipeline starts.
        """
    @property
    def initialConfig(self) -> depthai.beta.HRNetParserConfig:
        """Configuration used until a message is received on inputConfig.
        (self: depthai.beta.node.HRNetParser) -> depthai.beta.HRNetParserConfig
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with heatmaps data to parse.
        (self: depthai.beta.node.HRNetParser) -> depthai.Node.Input
        """
    @property
    def inputConfig(self) -> depthai.Node.Input:
        """Runtime parser configuration. When synchronized, one configuration is consumed
        per frame; otherwise all queued configurations are drained and the newest valid
        one is used.
        (self: depthai.beta.node.HRNetParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs Keypoints message with the detected body keypoints.
        (self: depthai.beta.node.HRNetParser) -> depthai.Node.Output
        """

class ImageOutputParser(depthai.DeviceNode):
    """ImageOutputParser node. Parses the output of image-to-image models (e.g. DnCNN3,
    zero-dce) where the output is a modified image (denoised, enhanced etc.) into a
    dai::ImgFrame message.

    The parser consumes a single output tensor. When the incoming NNData contains
    exactly one tensor, it is selected automatically; otherwise the output layer
    name must be configured explicitly or through an NNArchive head. The tensor is
    read in its stored order; after squeezing a leading batch dimension of 1 it must
    be a 3D image tensor in CHW or HWC orientation, with the channel dimension equal
    to 1 (grayscale) or 3 (color). All dimensions are derived from the runtime
    tensor descriptor. The values are min-max normalized and scaled to the [0, 255]
    8-bit range.

    A grayscale image is emitted as a GRAY8 frame. A color image is emitted as a
    BGR888p frame when the pipeline's default device platform is RVC2 and as a
    BGR888i frame otherwise, including in a device-less pipeline. The model output
    is treated as RGB and converted to BGR unless the BGR-output flag marks it as
    already BGR."""
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> ImageOutputParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.ImageOutputParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.ImageOutputParser

        Build ImageOutputParser node. Links the supplied output to this node's input and
        configures the parser from the model's ImageOutputParser head.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.ImageOutputParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.ImageOutputParser

        Build ImageOutputParser node with the specific head from an NNArchive. Useful
        when the model has multiple heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser
        """
    def getBGROutput(self) -> bool:
        """getBGROutput(self: depthai.beta.node.ImageOutputParser) -> bool

        Returns the flag indicating whether the model output image is in BGR (Blue-
        Green-Red) channel order.
        """
    def getOutputLayerName(self) -> str:
        """getOutputLayerName(self: depthai.beta.node.ImageOutputParser) -> str

        Returns the name of the model output layer to parse.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.ImageOutputParser) -> bool

        Returns true when this node runs on the host.

        Host-only pipelines always run the node on the host.
        """
    def setBGROutput(self, outputIsBGR: bool = ...) -> None:
        """setBGROutput(self: depthai.beta.node.ImageOutputParser, outputIsBGR: bool = True) -> None

        Sets the flag indicating whether the model output image is in BGR (Blue-Green-
        Red) channel order.

        When false (the default), a color model output is treated as RGB and its
        channels are swapped to BGR before being emitted.

        Parameter ``outputIsBGR``:
            True when the model output image is already BGR, defaults to true
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.ImageOutputParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one
        ImageOutputParser head; use setNNArchiveHead() to select a specific head from a
        multi-head archive.

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        """setNNArchiveHead(self: depthai.beta.node.ImageOutputParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head must be an ImageOutputParser head
        with exactly one output layer.

        Parameter ``head:``:
            NNArchive head to set
        """
    def setOutputLayerName(self, outputLayerName: str) -> None:
        """setOutputLayerName(self: depthai.beta.node.ImageOutputParser, outputLayerName: str) -> None

        Sets the name of the model output layer to parse.

        When left empty, the parser selects the tensor automatically if the incoming
        NNData contains exactly one tensor and fails otherwise.

        Parameter ``outputLayerName``:
            Name of the output layer
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.ImageOutputParser, runOnHost: bool) -> None

        Select whether the node runs on the host or device.
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with image tensor data to parse.
        (self: depthai.beta.node.ImageOutputParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs ImgFrame message with the model output image, e.g. a denoised or
        enhanced image.
        (self: depthai.beta.node.ImageOutputParser) -> depthai.Node.Output
        """

class ImgDetectionsFilter(depthai.DeviceNode):
    """Experimental node for filtering image detections."""
    Config: ClassVar[type[depthai.beta.ImgDetectionsFilterConfig]] = ...
    Properties: ClassVar[type[depthai.beta.ImgDetectionsFilterProperties]] = ...
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.ImgDetectionsFilter) -> bool

        Returns true when this node runs on the host.

        Host-only pipelines always run the node on the host.
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.ImgDetectionsFilter, runOnHost: bool) -> None

        Select whether the node runs on the host or device.
        """
    @property
    def initialConfig(self) -> depthai.beta.ImgDetectionsFilterConfig:
        """Configuration used until a message is received on inputConfig.

        The default configuration forwards detections unchanged.
        (self: depthai.beta.node.ImgDetectionsFilter) -> depthai.beta.ImgDetectionsFilterConfig
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Image detections to filter.
        (self: depthai.beta.node.ImgDetectionsFilter) -> depthai.Node.Input
        """
    @property
    def inputConfig(self) -> depthai.Node.Input:
        """Runtime filter configuration. The most recently received configuration is reused
        for subsequent detection messages.
        (self: depthai.beta.node.ImgDetectionsFilter) -> depthai.Node.Input
        """
    @property
    def output(self) -> depthai.Node.Output:
        """Filtered image detections.
        (self: depthai.beta.node.ImgDetectionsFilter) -> depthai.Node.Output
        """

class KeypointParser(depthai.DeviceNode):
    """KeypointParser node. Parses the raw output of a 2D or 3D keypoints neural
    network into a dai::beta::Keypoints message.

    The parser consumes a single output tensor. When the incoming NNData contains
    exactly one tensor, it is selected automatically; otherwise the output layer
    name must be configured explicitly or through an NNArchive head. The number of
    keypoints must be configured before the pipeline starts. The number of
    coordinates per keypoint (2 or 3) is derived from the tensor size and the
    configured number of keypoints. Keypoint coordinates are divided by the
    configured scale factor and clipped to [0, 1]."""
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> KeypointParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.KeypointParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.KeypointParser

        Build KeypointParser node. Links the supplied output to this node's input and
        configures the parser from the model's keypoints head.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.KeypointParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.KeypointParser

        Build KeypointParser node with the specific head from an NNArchive. Useful when
        the model has multiple heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser
        """
    def getEdges(self, *args, **kwargs):
        """getEdges(self: depthai.beta.node.KeypointParser) -> list[Annotated[list[int], FixedSize(2)]]

        Returns the skeleton edges as pairs of keypoint indices.
        """
    def getLabelNames(self) -> list[str]:
        """getLabelNames(self: depthai.beta.node.KeypointParser) -> list[str]

        Returns the label names for the keypoints.
        """
    def getNumKeypoints(self) -> int | None:
        """getNumKeypoints(self: depthai.beta.node.KeypointParser) -> Optional[int]

        Returns the number of keypoints the model detects, or std::nullopt when not
        configured.
        """
    def getOutputLayerName(self) -> str:
        """getOutputLayerName(self: depthai.beta.node.KeypointParser) -> str

        Returns the name of the model output layer to parse.
        """
    def getScaleFactor(self) -> float:
        """getScaleFactor(self: depthai.beta.node.KeypointParser) -> float

        Returns the scale factor to divide the keypoint coordinates by.
        """
    def getScoreThreshold(self) -> float | None:
        """getScoreThreshold(self: depthai.beta.node.KeypointParser) -> Optional[float]

        Returns the confidence score threshold for detected keypoints, or std::nullopt
        when not configured.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.KeypointParser) -> bool

        Returns true when this node runs on the host.

        Host-only pipelines always run the node on the host.
        """
    def setEdges(self, edges) -> None:
        """setEdges(self: depthai.beta.node.KeypointParser, edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        Sets the skeleton edges as pairs of keypoint indices used for visualizing the
        skeleton.

        Example: {{0, 1}, {1, 2}, {2, 3}, {3, 0}} connects keypoint 0 to keypoint 1,
        keypoint 1 to keypoint 2, etc.

        Parameter ``edges``:
            Vector of keypoint index pairs
        """
    def setLabelNames(self, labelNames: list[str]) -> None:
        """setLabelNames(self: depthai.beta.node.KeypointParser, labelNames: list[str]) -> None

        Sets the label names for the keypoints, indexed by keypoint index.

        Parameter ``labelNames``:
            Vector of label names
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.KeypointParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one KeypointParser
        head; use setNNArchiveHead() to select a specific head from a multi-head
        archive.

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        """setNNArchiveHead(self: depthai.beta.node.KeypointParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head must be a KeypointParser head with
        exactly one output layer.

        Parameter ``head:``:
            NNArchive head to set
        """
    def setNumKeypoints(self, nKeypoints: int) -> None:
        """setNumKeypoints(self: depthai.beta.node.KeypointParser, nKeypoints: int) -> None

        Sets the number of keypoints the model detects.

        Must be configured before the pipeline starts, either explicitly or through an
        NNArchive head.

        Parameter ``nKeypoints``:
            Number of keypoints, must be greater than 0
        """
    def setOutputLayerName(self, outputLayerName: str) -> None:
        """setOutputLayerName(self: depthai.beta.node.KeypointParser, outputLayerName: str) -> None

        Sets the name of the model output layer to parse.

        When left empty, the parser selects the tensor automatically if the incoming
        NNData contains exactly one tensor and fails otherwise.

        Parameter ``outputLayerName``:
            Name of the output layer
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.KeypointParser, runOnHost: bool) -> None

        Select whether the node runs on the host or device.
        """
    def setScaleFactor(self, scaleFactor: float) -> None:
        """setScaleFactor(self: depthai.beta.node.KeypointParser, scaleFactor: float) -> None

        Sets the scale factor to divide the keypoint coordinates by.

        Parameter ``scaleFactor``:
            Scale factor, must be greater than 0
        """
    def setScoreThreshold(self, threshold: float) -> None:
        """setScoreThreshold(self: depthai.beta.node.KeypointParser, threshold: float) -> None

        Sets the confidence score threshold for detected keypoints.

        Parameter ``threshold``:
            Confidence score threshold, must be between 0 and 1
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with keypoints data to parse.
        (self: depthai.beta.node.KeypointParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs Keypoints message with the parsed 2D or 3D keypoints.
        (self: depthai.beta.node.KeypointParser) -> depthai.Node.Output
        """

class LaneDetectionParser(depthai.DeviceNode):
    """LaneDetectionParser node. Parses the output of an Ultra-Fast-Lane-Detection
    (UFLD) neural network, e.g. the CULane and TuSimple variants, into a
    dai::beta::Clusters message with one cluster of normalized points per lane,
    including empty clusters for lanes without enough detected points.

    The parser consumes a single output tensor. When the incoming NNData contains
    exactly one tensor, it is selected automatically; otherwise the output layer
    name must be configured explicitly or through an NNArchive head. The tensor is
    read in its stored order and must be a 4D tensor of shape (batch, gridingNum +
    1, clsNumPerLane, numLanes); the first batch entry is decoded. The row anchors,
    griding number and number of points per lane must be configured before the
    pipeline starts, either explicitly or through an NNArchive head. The input size
    must also be configured before the pipeline starts: building from a full
    NNArchive derives it from the model input's declared shape and layout (NHWC or
    NCHW), while building from a specific head requires setInputSize() because a
    head carries no model input metadata."""
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> LaneDetectionParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.LaneDetectionParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.LaneDetectionParser

        Build LaneDetectionParser node. Links the supplied output to this node's input
        and configures the parser from the model's LaneDetectionParser head and the
        model input's declared shape and layout.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.LaneDetectionParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.LaneDetectionParser

        Build LaneDetectionParser node with the specific head from an NNArchive. Useful
        when the model has multiple heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser @note A head carries no
            model input metadata, so the input size must additionally be configured with
            setInputSize().
        """
    def getClsNumPerLane(self) -> int | None:
        """getClsNumPerLane(self: depthai.beta.node.LaneDetectionParser) -> Optional[int]

        Returns the number of points per lane, or std::nullopt when not configured.
        """
    def getGridingNum(self) -> int | None:
        """getGridingNum(self: depthai.beta.node.LaneDetectionParser) -> Optional[int]

        Returns the griding number, or std::nullopt when not configured.
        """
    def getInputSize(self) -> tuple[int, int] | None:
        """getInputSize(self: depthai.beta.node.LaneDetectionParser) -> Optional[tuple[int, int]]

        Returns the model input image size as (width, height), or std::nullopt when not
        configured.
        """
    def getOutputLayerName(self) -> str:
        """getOutputLayerName(self: depthai.beta.node.LaneDetectionParser) -> str

        Returns the name of the model output layer to parse.
        """
    def getRowAnchors(self) -> list[int]:
        """getRowAnchors(self: depthai.beta.node.LaneDetectionParser) -> list[int]

        Returns the row anchors, or an empty vector when not configured.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.LaneDetectionParser) -> bool

        Returns true when this node runs on the host.

        Host-only pipelines always run the node on the host.
        """
    def setClsNumPerLane(self, clsNumPerLane: int) -> None:
        """setClsNumPerLane(self: depthai.beta.node.LaneDetectionParser, clsNumPerLane: int) -> None

        Sets the number of points per lane.

        Must be configured before the pipeline starts, either explicitly or through an
        NNArchive head.

        Parameter ``clsNumPerLane``:
            Number of points per lane, must be greater than 0
        """
    def setGridingNum(self, gridingNum: int) -> None:
        """setGridingNum(self: depthai.beta.node.LaneDetectionParser, gridingNum: int) -> None

        Sets the griding number, the number of column samples the model predicts lane
        positions over.

        Must be configured before the pipeline starts, either explicitly or through an
        NNArchive head.

        Parameter ``gridingNum``:
            Griding number, must be greater than 1
        """
    def setInputSize(self, width: int, height: int) -> None:
        """setInputSize(self: depthai.beta.node.LaneDetectionParser, width: int, height: int) -> None

        Sets the model input image size the emitted points are computed against and
        normalized by.

        Must be configured before the pipeline starts. Configuring from a full NNArchive
        derives it from the model input's declared shape and layout; the most recent
        configuration wins.

        Parameter ``width``:
            Input image width, must be greater than 0

        Parameter ``height``:
            Input image height, must be greater than 0
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.LaneDetectionParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one
        LaneDetectionParser head and exactly one model input; use setNNArchiveHead() to
        select a specific head from a multi-head archive. The input size is derived from
        the model input's declared shape and layout (NHWC or NCHW).

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        """setNNArchiveHead(self: depthai.beta.node.LaneDetectionParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head must be a LaneDetectionParser head
        with exactly one output layer.

        Parameter ``head:``:
            NNArchive head to set @note A head carries no model input metadata, so the
            input size must additionally be configured with setInputSize().
        """
    def setOutputLayerName(self, outputLayerName: str) -> None:
        """setOutputLayerName(self: depthai.beta.node.LaneDetectionParser, outputLayerName: str) -> None

        Sets the name of the model output layer to parse.

        When left empty, the parser selects the tensor automatically if the incoming
        NNData contains exactly one tensor and fails otherwise.

        Parameter ``outputLayerName``:
            Name of the output layer
        """
    def setRowAnchors(self, rowAnchors: list[int]) -> None:
        """setRowAnchors(self: depthai.beta.node.LaneDetectionParser, rowAnchors: list[int]) -> None

        Sets the row anchors, the image rows at which the model predicts lane positions.

        Must be configured before the pipeline starts, either explicitly or through an
        NNArchive head, and must contain at least as many entries as the number of
        points per lane.

        Parameter ``rowAnchors``:
            Row anchors, must not be empty
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.LaneDetectionParser, runOnHost: bool) -> None

        Select whether the node runs on the host or device.
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with lane detection data to parse.
        (self: depthai.beta.node.LaneDetectionParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs Clusters message with the detected lanes represented as clusters of
        points.
        (self: depthai.beta.node.LaneDetectionParser) -> depthai.Node.Output
        """

class MLSDParser(depthai.DeviceNode):
    """MLSDParser node. Parses the output of the M-LSD line segment detection model
    into a dai::beta::Lines message with the detected lines and their confidence
    scores, ordered by descending score.

    The parser consumes two output tensors that must be configured before the
    pipeline starts, either explicitly or through an NNArchive head: the tpMap
    tensor, read in NCHW orientation as a 4D tensor of shape (batch, channels,
    height, width) whose channels 1 to 4 hold the line displacement maps of the
    first batch entry, and the heat tensor, flattened to one score per (height,
    width) grid position. The topK highest-scoring grid positions are decoded into
    candidate lines and kept when their score and length are strictly above the
    score and distance thresholds. Ties between equal heat scores are ordered
    following numpy's portable argpartition/argsort semantics. The emitted line
    coordinates are normalized by the model input size, which defaults to 512x512
    (the input size of all known M-LSD models, hard-coded by the source parser);
    building from a full NNArchive derives it from the model input's declared shape
    and layout (NHWC or NCHW), and setInputSize() overrides it."""
    Properties: ClassVar[type[depthai.beta.MLSDParserProperties]] = ...
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> MLSDParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.MLSDParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.MLSDParser

        Build MLSDParser node. Links the supplied output to this node's input and
        configures the parser from the model's MLSDParser head and the model input's
        declared shape and layout.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.MLSDParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.MLSDParser

        Build MLSDParser node with the specific head from an NNArchive. Useful when the
        model has multiple heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser @note A head carries no
            model input metadata, so the input size keeps its current value (512x512 by
            default); use setInputSize() for models with a different input size.
        """
    def getDistanceThreshold(self) -> float:
        """getDistanceThreshold(self: depthai.beta.node.MLSDParser) -> float

        Returns the distance threshold for detected lines.
        """
    def getInputSize(self) -> tuple[int, int]:
        """getInputSize(self: depthai.beta.node.MLSDParser) -> tuple[int, int]

        Returns the model input image size as (width, height).
        """
    def getOutputLayerHeat(self) -> str:
        """getOutputLayerHeat(self: depthai.beta.node.MLSDParser) -> str

        Returns the name of the output layer containing the heat tensor, or an empty
        string when not configured.
        """
    def getOutputLayerTPMap(self) -> str:
        """getOutputLayerTPMap(self: depthai.beta.node.MLSDParser) -> str

        Returns the name of the output layer containing the tpMap tensor, or an empty
        string when not configured.
        """
    def getScoreThreshold(self) -> float:
        """getScoreThreshold(self: depthai.beta.node.MLSDParser) -> float

        Returns the confidence score threshold for detected lines.
        """
    def getTopK(self) -> int:
        """getTopK(self: depthai.beta.node.MLSDParser) -> int

        Returns the number of top candidates to keep.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.MLSDParser) -> bool

        Returns true when this node runs on the host.

        Host-only pipelines always run the node on the host.
        """
    def setDistanceThreshold(self, distanceThreshold: float) -> None:
        """setDistanceThreshold(self: depthai.beta.node.MLSDParser, distanceThreshold: float) -> None

        Sets the distance threshold for detected lines. Candidates whose length in heat
        map grid units is strictly above the threshold are kept.

        Parameter ``distanceThreshold``:
            Distance threshold @note Configures startup behavior. Send MLSDParserConfig
            to inputConfig after the pipeline starts.
        """
    def setInputSize(self, width: int, height: int) -> None:
        """setInputSize(self: depthai.beta.node.MLSDParser, width: int, height: int) -> None

        Sets the model input image size the emitted line coordinates are normalized by,
        x coordinates by the width and y coordinates by the height.

        Defaults to 512x512, the input size of all known M-LSD models. Configuring from
        a full NNArchive derives it from the model input's declared shape and layout;
        the most recent configuration wins.

        Parameter ``width``:
            Input image width, must be greater than 0

        Parameter ``height``:
            Input image height, must be greater than 0
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.MLSDParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one MLSDParser
        head and exactly one model input; use setNNArchiveHead() to select a specific
        head from a multi-head archive. The input size is derived from the model input's
        declared shape and layout (NHWC or NCHW).

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        '''setNNArchiveHead(self: depthai.beta.node.MLSDParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head must be an MLSDParser head with
        exactly two output layers; the layer whose name contains "tpMap" is used as the
        tpMap layer and the layer whose name contains "heat" as the heat layer.

        Parameter ``head:``:
            NNArchive head to set @note A head carries no model input metadata, so the
            input size keeps its current value (512x512 by default); use setInputSize()
            for models with a different input size.
        '''
    def setOutputLayerHeat(self, outputLayerHeat: str) -> None:
        """setOutputLayerHeat(self: depthai.beta.node.MLSDParser, outputLayerHeat: str) -> None

        Sets the name of the output layer containing the heat tensor.

        Must be configured before the pipeline starts, either explicitly or through an
        NNArchive head.

        Parameter ``outputLayerHeat``:
            Name of the output layer containing the heat tensor
        """
    def setOutputLayerTPMap(self, outputLayerTPMap: str) -> None:
        """setOutputLayerTPMap(self: depthai.beta.node.MLSDParser, outputLayerTPMap: str) -> None

        Sets the name of the output layer containing the tpMap tensor.

        Must be configured before the pipeline starts, either explicitly or through an
        NNArchive head.

        Parameter ``outputLayerTPMap``:
            Name of the output layer containing the tpMap tensor
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.MLSDParser, runOnHost: bool) -> None

        Select whether the node runs on the host or device.
        """
    def setScoreThreshold(self, scoreThreshold: float) -> None:
        """setScoreThreshold(self: depthai.beta.node.MLSDParser, scoreThreshold: float) -> None

        Sets the confidence score threshold for detected lines. Candidates with a heat
        score strictly above the threshold are kept.

        Parameter ``scoreThreshold``:
            Confidence score threshold @note Configures startup behavior. Send
            MLSDParserConfig to inputConfig after the pipeline starts.
        """
    def setTopK(self, topK: int) -> None:
        """setTopK(self: depthai.beta.node.MLSDParser, topK: int) -> None

        Sets the number of top candidates to keep.

        The number of candidates is capped at the heat map size when decoding.

        Parameter ``topK``:
            Number of top candidates to keep, must be positive @note Configures startup
            behavior. Send MLSDParserConfig to inputConfig after the pipeline starts.
        """
    @property
    def initialConfig(self) -> depthai.beta.MLSDParserConfig:
        """Configuration used until a message is received on inputConfig.
        (self: depthai.beta.node.MLSDParser) -> depthai.beta.MLSDParserConfig
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with line detection data to parse.
        (self: depthai.beta.node.MLSDParser) -> depthai.Node.Input
        """
    @property
    def inputConfig(self) -> depthai.Node.Input:
        """Runtime parser configuration. When synchronized, one configuration is consumed
        per frame; otherwise all queued configurations are drained and the newest valid
        one is used.
        (self: depthai.beta.node.MLSDParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs Lines message with the detected lines and confidence scores.
        (self: depthai.beta.node.MLSDParser) -> depthai.Node.Output
        """

class MPPalmDetectionParser(depthai.DeviceNode):
    """MPPalmDetectionParser node. Parses the output of the MediaPipe palm detection
    model into a dai::ImgDetections message containing the rotated bounding boxes,
    labels and confidence scores of the detected hands. The decoding is based on
    https://github.com/geaxgx/depthai_hand_tracker (MIT License).

    The parser consumes two output tensors and identifies them by their last
    dimension: the tensor with the larger last dimension holds the raw bounding
    boxes and is reshaped to (numAnchors, 18) rows of bounding box center/size plus
    7 palm keypoint coordinate pairs; the tensor with the smaller last dimension
    holds the raw scores and is flattened to (numAnchors,). The scores are passed
    through a sigmoid and filtered with the confidence threshold, the kept rows are
    decoded against the model's SSD anchors generated from the configured scale (the
    model input size), converted to rectangles rotated to align the wrist to middle-
    finger direction with the rectangle's y-axis and expanded to squares, and non-
    maximum suppression keeps at most the configured maximum number of detections.
    The emitted bounding boxes are normalized to [0, 1]."""
    Properties: ClassVar[type[depthai.beta.MPPalmDetectionParserProperties]] = ...
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> MPPalmDetectionParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.MPPalmDetectionParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.MPPalmDetectionParser

        Build MPPalmDetectionParser node. Links the supplied output to this node's input
        and configures the parser from the model's MPPalmDetectionParser head.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.MPPalmDetectionParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.MPPalmDetectionParser

        Build MPPalmDetectionParser node with the specific head from an NNArchive.
        Useful when the model has multiple heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser
        """
    def getConfidenceThreshold(self) -> float:
        """getConfidenceThreshold(self: depthai.beta.node.MPPalmDetectionParser) -> float

        Returns the confidence score threshold for detected hands.
        """
    def getIouThreshold(self) -> float:
        """getIouThreshold(self: depthai.beta.node.MPPalmDetectionParser) -> float

        Returns the non-maximum suppression (IoU) threshold.
        """
    def getLabelNames(self) -> list[str]:
        """getLabelNames(self: depthai.beta.node.MPPalmDetectionParser) -> list[str]

        Returns the label names for the detected hands.
        """
    def getMaxDetections(self) -> int:
        """getMaxDetections(self: depthai.beta.node.MPPalmDetectionParser) -> int

        Returns the maximum number of detections to keep.
        """
    def getOutputLayerNames(self) -> list[str]:
        """getOutputLayerNames(self: depthai.beta.node.MPPalmDetectionParser) -> list[str]

        Returns the names of the model output layers relevant to the parser.
        """
    def getScale(self) -> int:
        """getScale(self: depthai.beta.node.MPPalmDetectionParser) -> int

        Returns the scale of the model input image in pixels.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.MPPalmDetectionParser) -> bool

        Returns true when this node runs on the host.

        Host-only pipelines always run the node on the host.
        """
    def setConfidenceThreshold(self, threshold: float) -> None:
        """setConfidenceThreshold(self: depthai.beta.node.MPPalmDetectionParser, threshold: float) -> None

        Sets the confidence score threshold for detected hands. Detections with a
        sigmoid score strictly above the threshold are kept.

        Parameter ``threshold``:
            Confidence score threshold @note Configures startup behavior. Send
            MPPalmDetectionParserConfig to inputConfig after the pipeline starts.
        """
    def setIouThreshold(self, threshold: float) -> None:
        """setIouThreshold(self: depthai.beta.node.MPPalmDetectionParser, threshold: float) -> None

        Sets the non-maximum suppression (IoU) threshold.

        Parameter ``threshold``:
            Non-maximum suppression threshold @note Configures startup behavior. Send
            MPPalmDetectionParserConfig to inputConfig after the pipeline starts.
        """
    def setLabelNames(self, labelNames: list[str]) -> None:
        """setLabelNames(self: depthai.beta.node.MPPalmDetectionParser, labelNames: list[str]) -> None

        Sets the label names for the detected hands. The first label name is assigned to
        every detection (all detections carry label 0). When empty, no label name is
        assigned.

        Parameter ``labelNames``:
            List of label names
        """
    def setMaxDetections(self, maxDetections: int) -> None:
        """setMaxDetections(self: depthai.beta.node.MPPalmDetectionParser, maxDetections: int) -> None

        Sets the maximum number of detections to keep.

        Parameter ``maxDetections``:
            Maximum number of detections to keep @note Configures startup behavior. Send
            MPPalmDetectionParserConfig to inputConfig after the pipeline starts.
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.MPPalmDetectionParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one
        MPPalmDetectionParser head; use setNNArchiveHead() to select a specific head
        from a multi-head archive.

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        """setNNArchiveHead(self: depthai.beta.node.MPPalmDetectionParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head must be an MPPalmDetectionParser head
        with exactly two output layers.

        Parameter ``head:``:
            NNArchive head to set
        """
    def setOutputLayerNames(self, outputLayerNames: list[str]) -> None:
        """setOutputLayerNames(self: depthai.beta.node.MPPalmDetectionParser, outputLayerNames: list[str]) -> None

        Sets the names of the model output layers relevant to the parser. Exactly two
        layer names are required.

        Parameter ``outputLayerNames``:
            Names of the output layers
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.MPPalmDetectionParser, runOnHost: bool) -> None

        Select whether the node runs on the host or device.
        """
    def setScale(self, scale: int) -> None:
        """setScale(self: depthai.beta.node.MPPalmDetectionParser, scale: int) -> None

        Sets the scale of the model input image in pixels (e.g. 192 for a 192x192
        model). The SSD anchors used for decoding are generated from the scale; a scale
        that does not match the model input size fails decoding with an anchor count
        mismatch.

        Parameter ``scale``:
            Scale of the input image
        """
    @property
    def initialConfig(self) -> depthai.beta.MPPalmDetectionParserConfig:
        """Configuration used when the parser starts.
        (self: depthai.beta.node.MPPalmDetectionParser) -> depthai.beta.MPPalmDetectionParserConfig
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with palm detection data to parse.
        (self: depthai.beta.node.MPPalmDetectionParser) -> depthai.Node.Input
        """
    @property
    def inputConfig(self) -> depthai.Node.Input:
        """Runtime parser configuration. In synchronized mode one configuration is consumed
        per input frame; otherwise all queued configurations are drained and the newest
        valid one is retained.
        (self: depthai.beta.node.MPPalmDetectionParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs ImgDetections message with the rotated bounding boxes, labels and
        confidence scores of the detected hands.
        (self: depthai.beta.node.MPPalmDetectionParser) -> depthai.Node.Output
        """

class MapOutputParser(depthai.DeviceNode):
    """MapOutputParser node. Parses the output of models that produce map outputs, such
    as depth maps (e.g. DepthAnything), density maps (e.g. DM-Count), heat maps, and
    similar, into a dai::beta::Map2D message.

    The parser consumes a single output tensor. When the incoming NNData contains
    exactly one tensor, it is selected automatically; otherwise the output layer
    name must be configured explicitly or through an NNArchive head. The tensor is
    read in its stored order; leading dimensions of 1 are squeezed and the tensor
    must then be a 2D HW map, or a 3D HWN map with a singleton trailing dimension
    that is squeezed as well. All map dimensions are derived from the runtime tensor
    descriptor.

    When min-max scaling is enabled, the map values are scaled to the [0, 1] range;
    a constant map is left unchanged."""
    Properties: ClassVar[type[depthai.beta.MapOutputParserProperties]] = ...
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> MapOutputParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.MapOutputParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.MapOutputParser

        Build MapOutputParser node. Links the supplied output to this node's input and
        configures the parser from the model's MapOutputParser head.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.MapOutputParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.MapOutputParser

        Build MapOutputParser node with the specific head from an NNArchive. Useful when
        the model has multiple heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser
        """
    def getMinMaxScaling(self) -> bool:
        """getMinMaxScaling(self: depthai.beta.node.MapOutputParser) -> bool

        Returns the flag indicating whether the map is scaled to the [0, 1] range.
        """
    def getOutputLayerName(self) -> str:
        """getOutputLayerName(self: depthai.beta.node.MapOutputParser) -> str

        Returns the name of the model output layer to parse.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.MapOutputParser) -> bool

        Returns true when this node runs on the host.

        Host-only pipelines always run the node on the host.
        """
    def setMinMaxScaling(self, minMaxScaling: bool = ...) -> None:
        """setMinMaxScaling(self: depthai.beta.node.MapOutputParser, minMaxScaling: bool = True) -> None

        Sets the flag indicating whether the map is scaled to the [0, 1] range.

        When true, the map values are min-max scaled to [0, 1]; a constant map is left
        unchanged. Defaults to false.

        Parameter ``minMaxScaling``:
            True to scale the map to the [0, 1] range, defaults to true @note Configures
            startup behavior. Send MapOutputParserConfig to inputConfig after the
            pipeline starts.
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.MapOutputParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one
        MapOutputParser head; use setNNArchiveHead() to select a specific head from a
        multi-head archive.

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        """setNNArchiveHead(self: depthai.beta.node.MapOutputParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head must be a MapOutputParser head with
        exactly one output layer.

        Parameter ``head:``:
            NNArchive head to set
        """
    def setOutputLayerName(self, outputLayerName: str) -> None:
        """setOutputLayerName(self: depthai.beta.node.MapOutputParser, outputLayerName: str) -> None

        Sets the name of the model output layer to parse.

        When left empty, the parser selects the tensor automatically if the incoming
        NNData contains exactly one tensor and fails otherwise.

        Parameter ``outputLayerName``:
            Name of the output layer
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.MapOutputParser, runOnHost: bool) -> None

        Select whether the node runs on the host or device.
        """
    @property
    def initialConfig(self) -> depthai.beta.MapOutputParserConfig:
        """Configuration used until a message is received on inputConfig.
        (self: depthai.beta.node.MapOutputParser) -> depthai.beta.MapOutputParserConfig
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with map tensor data to parse.
        (self: depthai.beta.node.MapOutputParser) -> depthai.Node.Input
        """
    @property
    def inputConfig(self) -> depthai.Node.Input:
        """Runtime parser configuration. When synchronized, one configuration is consumed
        per frame; otherwise all queued configurations are drained and the newest valid
        one is used.
        (self: depthai.beta.node.MapOutputParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs Map2D message with the parsed 2D map, e.g. a depth or density map.
        (self: depthai.beta.node.MapOutputParser) -> depthai.Node.Output
        """

class PPTextDetectionParser(depthai.DeviceNode):
    """PPTextDetectionParser node. Parses the output of the PaddlePaddle OCR text
    detection model into a dai::ImgDetections message containing the rotated
    bounding boxes and confidence scores of the detected text.

    The parser consumes a single probability-map output tensor of shape (1, 1, H, W)
    or (1, H, W, 1). The map is thresholded with the mask threshold into a binary
    text mask, the mask is dilated and its contours become rotated-rectangle
    candidates; when more contours than the maximum number of detections remain, the
    largest by area are kept. Rectangles smaller than 8 pixels on their smaller side
    are dropped, each candidate is scored with the mean probability inside its
    (slightly shrunk) corner polygon, candidates scoring below the confidence
    threshold are dropped, and the kept rectangles are expanded by sqrt(2) in both
    dimensions. The emitted bounding boxes are normalized to [0, 1] with angles in
    degrees rounded to whole numbers; the detections carry no labels."""
    Properties: ClassVar[type[depthai.beta.PPTextDetectionParserProperties]] = ...
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> PPTextDetectionParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.PPTextDetectionParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.PPTextDetectionParser

        Build PPTextDetectionParser node. Links the supplied output to this node's input
        and configures the parser from the model's PPTextDetectionParser head.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.PPTextDetectionParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.PPTextDetectionParser

        Build PPTextDetectionParser node with the specific head from an NNArchive.
        Useful when the model has multiple heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser
        """
    def getConfidenceThreshold(self) -> float:
        """getConfidenceThreshold(self: depthai.beta.node.PPTextDetectionParser) -> float

        Returns the confidence score threshold for the detected text bounding boxes.
        """
    def getMaskThreshold(self) -> float:
        """getMaskThreshold(self: depthai.beta.node.PPTextDetectionParser) -> float

        Returns the mask threshold for creating the binary text mask from the model
        output probabilities.
        """
    def getMaxDetections(self) -> int:
        """getMaxDetections(self: depthai.beta.node.PPTextDetectionParser) -> int

        Returns the maximum number of candidate bounding boxes.
        """
    def getOutputLayerName(self) -> str:
        """getOutputLayerName(self: depthai.beta.node.PPTextDetectionParser) -> str

        Returns the name of the model output layer holding the text probability map.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.PPTextDetectionParser) -> bool

        Returns true when this node runs on the host.

        Host-only pipelines always run the node on the host.
        """
    def setConfidenceThreshold(self, threshold: float) -> None:
        """setConfidenceThreshold(self: depthai.beta.node.PPTextDetectionParser, threshold: float) -> None

        Sets the confidence score threshold for the detected text bounding boxes.
        Candidates with a score strictly below the threshold are dropped.

        Parameter ``threshold``:
            Confidence score threshold @note Configures startup behavior. Send
            PPTextDetectionParserConfig to inputConfig after the pipeline starts.
        """
    def setMaskThreshold(self, maskThreshold: float) -> None:
        """setMaskThreshold(self: depthai.beta.node.PPTextDetectionParser, maskThreshold: float) -> None

        Sets the mask threshold for creating the binary text mask from the model output
        probabilities. Probabilities strictly above the threshold belong to the mask.

        Parameter ``maskThreshold``:
            Mask threshold @note Configures startup behavior. Send
            PPTextDetectionParserConfig to inputConfig after the pipeline starts.
        """
    def setMaxDetections(self, maxDetections: int) -> None:
        """setMaxDetections(self: depthai.beta.node.PPTextDetectionParser, maxDetections: int) -> None

        Sets the maximum number of candidate bounding boxes. When more candidate
        contours are found, only the largest by area are kept.

        Parameter ``maxDetections``:
            Maximum number of candidate bounding boxes @note Configures startup
            behavior. Send PPTextDetectionParserConfig to inputConfig after the pipeline
            starts.
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.PPTextDetectionParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one
        PPTextDetectionParser head; use setNNArchiveHead() to select a specific head
        from a multi-head archive.

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        """setNNArchiveHead(self: depthai.beta.node.PPTextDetectionParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head configures the confidence threshold,
        the mask threshold and the maximum number of detections when present in its
        metadata. The head's declared output names are not consumed: the parser resolves
        the single runtime output tensor by itself (or uses the explicitly configured
        output layer name), mirroring the source parser; archives whose head output name
        differs from the model's declared output name therefore parse correctly.

        Parameter ``head:``:
            NNArchive head to set
        """
    def setOutputLayerName(self, outputLayerName: str) -> None:
        """setOutputLayerName(self: depthai.beta.node.PPTextDetectionParser, outputLayerName: str) -> None

        Sets the name of the model output layer holding the text probability map. When
        empty (the default), the layer is resolved automatically from single-tensor NN
        results; multi-tensor results require an explicit name.

        Parameter ``outputLayerName``:
            Name of the output layer
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.PPTextDetectionParser, runOnHost: bool) -> None

        Select whether the node runs on the host or device.
        """
    @property
    def initialConfig(self) -> depthai.beta.PPTextDetectionParserConfig:
        """Configuration used when the parser starts.
        (self: depthai.beta.node.PPTextDetectionParser) -> depthai.beta.PPTextDetectionParserConfig
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with text detection probability map to parse.
        (self: depthai.beta.node.PPTextDetectionParser) -> depthai.Node.Input
        """
    @property
    def inputConfig(self) -> depthai.Node.Input:
        """Runtime parser configuration. In synchronized mode one configuration is consumed
        per input frame; otherwise all queued configurations are drained and the newest
        valid one is retained.
        (self: depthai.beta.node.PPTextDetectionParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs ImgDetections message with the rotated bounding boxes and confidence
        scores of the detected text.
        (self: depthai.beta.node.PPTextDetectionParser) -> depthai.Node.Output
        """

class RFDETRParser(depthai.DeviceNode):
    """RFDETRParser node. Parses the output of RF-DETR object detection models
    (https://github.com/roboflow/rf-detr) into a dai::ImgDetections message
    containing the bounding boxes, labels, confidence scores and, in segmentation
    mode, an instance segmentation mask, everything normalized to [0, 1].

    The parser consumes 2 output tensors for detection (boxes, class logits) or 3
    for instance segmentation (boxes, class logits, mask logits), in that order.
    When no output layer names are configured, all layer names of the incoming
    NNData are used in their reported order. The boxes tensor squeezes to (N, 4)
    with normalized (xCenter, yCenter, width, height) boxes, the logits tensor is
    (1, N, C) and the mask logits tensor squeezes to (N, maskHeight, maskWidth).
    Class probabilities are the sigmoid of the logits; per query the maximum
    probability is the score and its class the label. Detections are ordered by
    descending score, truncated to the maximum number of detections and kept when
    their score is strictly greater than the confidence threshold.

    In segmentation mode at most 255 instances fit into the mask, so the truncation
    is additionally capped at 255. Each detection's mask logits are passed through a
    sigmoid, cropped to its bounding box, binarized with the mask confidence
    threshold and resized to the model input size with nearest-neighbor
    interpolation; the pixels not claimed by an earlier (higher-scoring) detection
    receive the detection's index, with 255 marking background."""
    Properties: ClassVar[type[depthai.beta.RFDETRParserProperties]] = ...
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> RFDETRParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.RFDETRParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.RFDETRParser

        Build RFDETRParser node. Links the supplied output to this node's input and
        configures the parser from the model's RFDETRParser head and the model input's
        declared shape and layout.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.RFDETRParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.RFDETRParser

        Build RFDETRParser node with the specific head from an NNArchive. Useful when
        the model has multiple heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser @note A head carries no
            model input metadata, so the input size keeps its current value;
            segmentation mode requires it to be configured with setInputSize() when it
            is not set yet.
        """
    def getConfidenceThreshold(self) -> float:
        """getConfidenceThreshold(self: depthai.beta.node.RFDETRParser) -> float

        Returns the confidence score threshold for detected objects.
        """
    def getInputSize(self) -> tuple[int, int] | None:
        """getInputSize(self: depthai.beta.node.RFDETRParser) -> Optional[tuple[int, int]]

        Returns the model input image size as (width, height), or std::nullopt when it
        is not set.
        """
    def getLabelNames(self) -> list[str]:
        """getLabelNames(self: depthai.beta.node.RFDETRParser) -> list[str]

        Returns the label names for the detected objects.
        """
    def getMaskConfidence(self) -> float:
        """getMaskConfidence(self: depthai.beta.node.RFDETRParser) -> float

        Returns the mask confidence threshold.
        """
    def getMaxDetections(self) -> int:
        """getMaxDetections(self: depthai.beta.node.RFDETRParser) -> int

        Returns the maximum number of detections to keep.
        """
    def getOutputLayerNames(self) -> list[str]:
        """getOutputLayerNames(self: depthai.beta.node.RFDETRParser) -> list[str]

        Returns the names of the model output layers.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.RFDETRParser) -> bool

        Returns true when this node runs on the host.

        Host-only pipelines always run the node on the host.
        """
    def setConfidenceThreshold(self, threshold: float) -> None:
        """setConfidenceThreshold(self: depthai.beta.node.RFDETRParser, threshold: float) -> None

        Sets the confidence score threshold for detected objects. Detections with a
        score strictly greater than the threshold are kept. Defaults to 0.5.

        Parameter ``threshold``:
            Confidence score threshold, must be between 0 and 1 @note Configures startup
            behavior. Send RFDETRParserConfig to inputConfig after the pipeline starts.
        """
    def setInputSize(self, width: int, height: int) -> None:
        """setInputSize(self: depthai.beta.node.RFDETRParser, width: int, height: int) -> None

        Sets the model input image size the segmentation mask is emitted at. Unset by
        default; segmentation mode requires it to be configured from an NNArchive or
        with this setter before the parser processes messages. Detection mode does not
        use it.

        Configuring from a full NNArchive derives it from the first model input's
        declared shape and layout; the most recent configuration wins.

        Parameter ``width``:
            Input image width, must be greater than 0

        Parameter ``height``:
            Input image height, must be greater than 0
        """
    def setLabelNames(self, labelNames: list[str]) -> None:
        '''setLabelNames(self: depthai.beta.node.RFDETRParser, labelNames: list[str]) -> None

        Sets the label names for the detected objects, indexed by the class label. A
        detection whose label is out of range receives the name "class_<label>". When
        empty, no label names are assigned. Defaults to empty.

        Parameter ``labelNames``:
            List of label names
        '''
    def setMaskConfidence(self, maskConfidence: float) -> None:
        """setMaskConfidence(self: depthai.beta.node.RFDETRParser, maskConfidence: float) -> None

        Sets the mask confidence threshold used to binarize instance segmentation masks
        in segmentation mode. Mask pixels with a sigmoid probability strictly greater
        than the threshold belong to the instance. Defaults to 0.5.

        Parameter ``maskConfidence``:
            Mask confidence threshold, must be between 0 and 1 @note Configures startup
            behavior. Send RFDETRParserConfig to inputConfig after the pipeline starts.
        """
    def setMaxDetections(self, maxDetections: int) -> None:
        """setMaxDetections(self: depthai.beta.node.RFDETRParser, maxDetections: int) -> None

        Sets the maximum number of detections to keep, applied to the detections ordered
        by descending score. In segmentation mode the applied limit is additionally
        capped at 255, the maximum number of instances the segmentation mask can encode.
        Defaults to 300.

        Parameter ``maxDetections``:
            Maximum number of detections to keep, must be greater than 0 @note
            Configures startup behavior. Send RFDETRParserConfig to inputConfig after
            the pipeline starts.
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.RFDETRParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one RFDETRParser
        head; use setNNArchiveHead() to select a specific head from a multi-head
        archive. The input size is derived from the first model input's declared shape
        and layout (NHWC or NCHW).

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        """setNNArchiveHead(self: depthai.beta.node.RFDETRParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head must be an RFDETRParser head with 2
        output layers (boxes, class logits) for detection or 3 (boxes, class logits,
        mask logits) for segmentation.

        Parameter ``head:``:
            NNArchive head to set @note A head carries no model input metadata, so the
            input size keeps its current value; segmentation mode requires it to be
            configured with setInputSize() when it is not set yet.
        """
    def setOutputLayerNames(self, outputLayerNames: list[str]) -> None:
        """setOutputLayerNames(self: depthai.beta.node.RFDETRParser, outputLayerNames: list[str]) -> None

        Sets the names of the model output layers, positionally: the boxes layer, the
        class logits layer and, in segmentation mode, the mask logits layer. Must hold 2
        or 3 names.

        When left empty, all layer names of the incoming NNData are used in their
        reported order.

        Parameter ``outputLayerNames``:
            Names of the output layers
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.RFDETRParser, runOnHost: bool) -> None

        Select whether the node runs on the host or device.
        """
    @property
    def initialConfig(self) -> depthai.beta.RFDETRParserConfig:
        """Configuration used when the parser starts.
        (self: depthai.beta.node.RFDETRParser) -> depthai.beta.RFDETRParserConfig
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with RF-DETR detection data to parse.
        (self: depthai.beta.node.RFDETRParser) -> depthai.Node.Input
        """
    @property
    def inputConfig(self) -> depthai.Node.Input:
        """Runtime parser configuration. In synchronized mode one configuration is consumed
        per input frame; otherwise all queued configurations are drained and the newest
        valid one is retained.
        (self: depthai.beta.node.RFDETRParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs ImgDetections message with the bounding boxes, labels and confidence
        scores of the detected objects and, in segmentation mode, the instance
        segmentation mask.
        (self: depthai.beta.node.RFDETRParser) -> depthai.Node.Output
        """

class RegressionParser(depthai.DeviceNode):
    """RegressionParser node. Parses the output of a model with regression output (e.g.
    age-gender) into a dai::beta::Predictions message with the predicted value(s) in
    the order the model emitted them.

    The parser consumes a single output tensor. When the incoming NNData contains
    exactly one tensor, it is selected automatically; otherwise the output layer
    name must be configured explicitly or through an NNArchive head. The tensor is
    dequantized and all its singleton dimensions are squeezed; the remaining values
    become the predictions, so any tensor with at most one non-singleton dimension
    is accepted regardless of rank (for example (1, 1, 1, 3), (1, 1) or (1,)) and an
    empty tensor yields a message with no predictions. A tensor with more than one
    non-singleton dimension after squeezing is rejected."""
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> RegressionParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.RegressionParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.RegressionParser

        Build RegressionParser node. Links the supplied output to this node's input and
        configures the parser from the model's RegressionParser head.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.RegressionParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.RegressionParser

        Build RegressionParser node with the specific head from an NNArchive. Useful
        when the model has multiple regression heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser
        """
    def getOutputLayerName(self) -> str:
        """getOutputLayerName(self: depthai.beta.node.RegressionParser) -> str

        Returns the name of the model output layer to parse.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.RegressionParser) -> bool

        Returns true when this node runs on the host.

        Host-only pipelines always run the node on the host.
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.RegressionParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one
        RegressionParser head; use setNNArchiveHead() to select a specific head from a
        multi-head archive.

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        """setNNArchiveHead(self: depthai.beta.node.RegressionParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head must be a RegressionParser head with
        exactly one output layer.

        Parameter ``head:``:
            NNArchive head to set
        """
    def setOutputLayerName(self, outputLayerName: str) -> None:
        """setOutputLayerName(self: depthai.beta.node.RegressionParser, outputLayerName: str) -> None

        Sets the name of the model output layer to parse.

        When left empty, the parser selects the tensor automatically if the incoming
        NNData contains exactly one tensor and fails otherwise.

        Parameter ``outputLayerName``:
            Name of the output layer
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.RegressionParser, runOnHost: bool) -> None

        Select whether the node runs on the host or device.
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with regression data to parse.
        (self: depthai.beta.node.RegressionParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs Predictions message with the predicted value(s).
        (self: depthai.beta.node.RegressionParser) -> depthai.Node.Output
        """

class SCRFDParser(depthai.DeviceNode):
    """SCRFDParser node. Parses the output of SCRFD detection models (e.g. SCRFD face
    and person detection) into a dai::ImgDetections message containing the bounding
    boxes, labels, confidence scores and 5 keypoints per detected object, everything
    normalized to [0, 1].

    The parser consumes three output tensors per configured feature stride, named
    score_{stride}, bbox_{stride} and kps_{stride}. The score tensor is flattened,
    the bbox tensor is paired 4 values per score (left, top, right, bottom distances
    from the anchor center) and the kps tensor 10 values per score (5 keypoint
    coordinate pairs), accepting both batched (1, N, C) and unbatched (N, C)
    tensors. Scores greater than or equal to the confidence threshold (inclusive)
    are kept, decoded against the anchor centers derived from the input size, the
    stride and the number of anchors, sorted by descending score and suppressed with
    the original SCRFD non-maximum suppression (+1 offset box areas, overlaps at
    most the IoU threshold survive). The anchor centers are cached across messages
    and refreshed when the input size, feature strides or number of anchors change."""
    Properties: ClassVar[type[depthai.beta.SCRFDParserProperties]] = ...
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> SCRFDParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.SCRFDParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.SCRFDParser

        Build SCRFDParser node. Links the supplied output to this node's input and
        configures the parser from the model's SCRFDParser head and the model input's
        declared shape and layout.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.SCRFDParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.SCRFDParser

        Build SCRFDParser node with the specific head from an NNArchive. Useful when the
        model has multiple heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser @note A head carries no
            model input metadata, so the input size keeps its current value; configure
            it with setInputSize() when it differs from the default.
        """
    def getConfidenceThreshold(self) -> float:
        """getConfidenceThreshold(self: depthai.beta.node.SCRFDParser) -> float

        Returns the confidence score threshold for detected objects.
        """
    def getFeatStrideFPN(self) -> list[int]:
        """getFeatStrideFPN(self: depthai.beta.node.SCRFDParser) -> list[int]

        Returns the feature strides of the FPN.
        """
    def getInputSize(self) -> tuple[int, int]:
        """getInputSize(self: depthai.beta.node.SCRFDParser) -> tuple[int, int]

        Returns the model input image size as (width, height).
        """
    def getIouThreshold(self) -> float:
        """getIouThreshold(self: depthai.beta.node.SCRFDParser) -> float

        Returns the non-maximum suppression (IoU) threshold.
        """
    def getLabelNames(self) -> list[str]:
        """getLabelNames(self: depthai.beta.node.SCRFDParser) -> list[str]

        Returns the label names for the detected objects.
        """
    def getMaxDetections(self) -> int:
        """getMaxDetections(self: depthai.beta.node.SCRFDParser) -> int

        Returns the maximum number of detections to keep.
        """
    def getNumAnchors(self) -> int:
        """getNumAnchors(self: depthai.beta.node.SCRFDParser) -> int

        Returns the number of anchors per feature map position.
        """
    def getOutputLayerNames(self) -> list[str]:
        """getOutputLayerNames(self: depthai.beta.node.SCRFDParser) -> list[str]

        Returns the names of the model output layers relevant to the parser.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.SCRFDParser) -> bool

        Returns true when this node runs on the host.

        Host-only pipelines always run the node on the host.
        """
    def setConfidenceThreshold(self, threshold: float) -> None:
        """setConfidenceThreshold(self: depthai.beta.node.SCRFDParser, threshold: float) -> None

        Sets the confidence score threshold for detected objects. Detections with a
        score greater than or equal to the threshold (inclusive) are kept.

        Parameter ``threshold``:
            Confidence score threshold @note Configures startup behavior. Send
            SCRFDParserConfig to inputConfig after the pipeline starts.
        """
    def setFeatStrideFPN(self, featStrideFpn: list[int]) -> None:
        """setFeatStrideFPN(self: depthai.beta.node.SCRFDParser, featStrideFpn: list[int]) -> None

        Sets the feature strides of the FPN. One score_{stride}, bbox_{stride} and
        kps_{stride} layer triple is parsed per stride. Defaults to (8, 16, 32).

        Parameter ``featStrideFpn``:
            Feature strides, every stride must be greater than 0
        """
    def setInputSize(self, width: int, height: int) -> None:
        """setInputSize(self: depthai.beta.node.SCRFDParser, width: int, height: int) -> None

        Sets the model input image size the anchor centers are computed against and the
        emitted coordinates are normalized by. Defaults to (640, 640).

        Configuring from a full NNArchive derives it from the model input's declared
        shape and layout; the most recent configuration wins.

        Parameter ``width``:
            Input image width, must be greater than 0

        Parameter ``height``:
            Input image height, must be greater than 0
        """
    def setIouThreshold(self, threshold: float) -> None:
        """setIouThreshold(self: depthai.beta.node.SCRFDParser, threshold: float) -> None

        Sets the non-maximum suppression (IoU) threshold. Candidates whose overlap with
        a kept detection is at most the threshold (inclusive) survive suppression.

        Parameter ``threshold``:
            Non-maximum suppression threshold @note Configures startup behavior. Send
            SCRFDParserConfig to inputConfig after the pipeline starts.
        """
    def setLabelNames(self, labelNames: list[str]) -> None:
        '''setLabelNames(self: depthai.beta.node.SCRFDParser, labelNames: list[str]) -> None

        Sets the label names for the detected objects. The first label name is assigned
        to every detection (all detections carry label 0). When empty, no label name is
        assigned. Defaults to ("Face").

        Parameter ``labelNames``:
            List of label names
        '''
    def setMaxDetections(self, maxDetections: int) -> None:
        """setMaxDetections(self: depthai.beta.node.SCRFDParser, maxDetections: int) -> None

        Sets the maximum number of detections to keep.

        Parameter ``maxDetections``:
            Maximum number of detections to keep @note Configures startup behavior. Send
            SCRFDParserConfig to inputConfig after the pipeline starts.
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.SCRFDParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one SCRFDParser
        head and exactly one model input; use setNNArchiveHead() to select a specific
        head from a multi-head archive. The input size is derived from the model input's
        declared shape and layout (NHWC or NCHW).

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        """setNNArchiveHead(self: depthai.beta.node.SCRFDParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head must be an SCRFDParser head with an
        equal number of score, bbox and kps output layers.

        Parameter ``head:``:
            NNArchive head to set @note A head carries no model input metadata, so the
            input size keeps its current value; configure it with setInputSize() when it
            differs from the default.
        """
    def setNumAnchors(self, numAnchors: int) -> None:
        """setNumAnchors(self: depthai.beta.node.SCRFDParser, numAnchors: int) -> None

        Sets the number of anchors per feature map position. Defaults to 2.

        Parameter ``numAnchors``:
            Number of anchors; values of 1 or less yield one anchor per position,
            mirroring the source behavior
        """
    def setOutputLayerNames(self, outputLayerNames: list[str]) -> None:
        """setOutputLayerNames(self: depthai.beta.node.SCRFDParser, outputLayerNames: list[str]) -> None

        Sets the names of the model output layers relevant to the parser. The parser
        looks up the per-stride score_{stride}, bbox_{stride} and kps_{stride} layer
        names in this list.

        When left empty, the list is resolved from the layer names of the first incoming
        NNData.

        Parameter ``outputLayerNames``:
            Names of the output layers
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.SCRFDParser, runOnHost: bool) -> None

        Select whether the node runs on the host or device.
        """
    @property
    def initialConfig(self) -> depthai.beta.SCRFDParserConfig:
        """Configuration used when the parser starts.
        (self: depthai.beta.node.SCRFDParser) -> depthai.beta.SCRFDParserConfig
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with SCRFD detection data to parse.
        (self: depthai.beta.node.SCRFDParser) -> depthai.Node.Input
        """
    @property
    def inputConfig(self) -> depthai.Node.Input:
        """Runtime parser configuration. In synchronized mode one configuration is consumed
        per input frame; otherwise all queued configurations are drained and the newest
        valid one is retained.
        (self: depthai.beta.node.SCRFDParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs ImgDetections message with the bounding boxes, labels, confidence scores
        and keypoints of the detected objects.
        (self: depthai.beta.node.SCRFDParser) -> depthai.Node.Output
        """

class Stitching(depthai.DeviceNode):
    """Stitching node. Combines N time-synced image streams into a single stitched
    image.

    The node runs on the host by default and can run on an RVC4 device when selected
    with `setRunOnHost(false)`. Host execution requires depthai-core OpenCV support.
    Inputs are fixed at build() time and synced by an internal Sync subnode that
    follows the node's execution side, so host-mode sources may come from different
    devices. Two independent stitching modes are available:

    - `Mode::PANORAMA` wraps OpenCV's cv::Stitcher and registers the images from
    their content, so no calibration is needed, but the cameras have to overlap. -
    `Mode::PLANAR_PROJECTION` projects the images onto a plane given in the common
    origin frame of the inputs (bird's-eye view), driven purely by the calibration
    carried in the messages, so it also works without overlap. All input
    transformations must have the same origin camera socket."""

    class CameraModel:
        """Camera projection model the panorama images are warped onto.

        Members:

          SPHERICAL

          PINHOLE

          CYLINDRICAL"""
        __members__: ClassVar[dict] = ...  # read-only
        CYLINDRICAL: ClassVar[Stitching.CameraModel] = ...
        PINHOLE: ClassVar[Stitching.CameraModel] = ...
        SPHERICAL: ClassVar[Stitching.CameraModel] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.beta.node.Stitching.CameraModel, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.beta.node.Stitching.CameraModel) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.beta.node.Stitching.CameraModel) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.beta.node.Stitching.CameraModel) -> int"""

    class Mode:
        """Stitching mode.

        Members:

          PANORAMA

          PLANAR_PROJECTION"""
        __members__: ClassVar[dict] = ...  # read-only
        PANORAMA: ClassVar[Stitching.Mode] = ...
        PLANAR_PROJECTION: ClassVar[Stitching.Mode] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.beta.node.Stitching.Mode, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.beta.node.Stitching.Mode) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.beta.node.Stitching.Mode) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.beta.node.Stitching.Mode) -> int"""

    class Plane:
        """A plane used by planar projection stitching."""
        normal: depthai.Point3f
        point: depthai.Point3f
        unit: depthai.LengthUnit
        @overload
        def __init__(self) -> None:
            """__init__(*args, **kwargs)
            Overloaded function.

            1. __init__(self: depthai.beta.node.Stitching.Plane) -> None

            2. __init__(self: depthai.beta.node.Stitching.Plane, point: depthai.Point3f, normal: depthai.Point3f, unit: depthai.LengthUnit = <LengthUnit.CENTIMETER: 1>) -> None
            """
        @overload
        def __init__(self, point: depthai.Point3f, normal: depthai.Point3f, unit: depthai.LengthUnit = ...) -> None:
            """__init__(*args, **kwargs)
            Overloaded function.

            1. __init__(self: depthai.beta.node.Stitching.Plane) -> None

            2. __init__(self: depthai.beta.node.Stitching.Plane, point: depthai.Point3f, normal: depthai.Point3f, unit: depthai.LengthUnit = <LengthUnit.CENTIMETER: 1>) -> None
            """

    class SeamFinder:
        """Seam estimation method.

        Members:

          NONE

          VORONOI

          DP_COLOR

          DP_COLOR_GRAD

          GRAPHCUT_COLOR

          GRAPHCUT_COLOR_GRAD"""
        __members__: ClassVar[dict] = ...  # read-only
        DP_COLOR: ClassVar[Stitching.SeamFinder] = ...
        DP_COLOR_GRAD: ClassVar[Stitching.SeamFinder] = ...
        GRAPHCUT_COLOR: ClassVar[Stitching.SeamFinder] = ...
        GRAPHCUT_COLOR_GRAD: ClassVar[Stitching.SeamFinder] = ...
        NONE: ClassVar[Stitching.SeamFinder] = ...
        VORONOI: ClassVar[Stitching.SeamFinder] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.beta.node.Stitching.SeamFinder, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.beta.node.Stitching.SeamFinder) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.beta.node.Stitching.SeamFinder) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.beta.node.Stitching.SeamFinder) -> int"""

    class VirtualCamera:
        """The pinhole camera used to render a planar projection."""
        height: int
        intrinsics: Incomplete
        pose: Incomplete
        unit: depthai.LengthUnit
        width: int
        def __init__(self) -> None:
            """__init__(self: depthai.beta.node.Stitching.VirtualCamera) -> None"""
    Properties: ClassVar[type[depthai.beta.StitchingProperties]] = ...
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    @overload
    def build(self, sources: list[depthai.Node.Output]) -> Stitching:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.Stitching, sources: list[depthai.Node.Output]) -> depthai.beta.node.Stitching

        Build the node with a fixed set of sources. The number of inputs cannot change
        afterwards.

        Parameter ``sources``:
            Outputs to stitch, at least two. Inputs are named input0..inputN-1 in this
            order

        2. build(self: depthai.beta.node.Stitching, numInputs: int) -> depthai.beta.node.Stitching

        Build the node with a fixed number of unlinked inputs, to be fed from host
        queues.

        Parameter ``numInputs``:
            Number of inputs to stitch, at least two. Inputs are named input0..inputN-1
        """
    @overload
    def build(self, numInputs: int) -> Stitching:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.Stitching, sources: list[depthai.Node.Output]) -> depthai.beta.node.Stitching

        Build the node with a fixed set of sources. The number of inputs cannot change
        afterwards.

        Parameter ``sources``:
            Outputs to stitch, at least two. Inputs are named input0..inputN-1 in this
            order

        2. build(self: depthai.beta.node.Stitching, numInputs: int) -> depthai.beta.node.Stitching

        Build the node with a fixed number of unlinked inputs, to be fed from host
        queues.

        Parameter ``numInputs``:
            Number of inputs to stitch, at least two. Inputs are named input0..inputN-1
        """
    def getCameraModel(self) -> Stitching.CameraModel:
        """getCameraModel(self: depthai.beta.node.Stitching) -> depthai.beta.node.Stitching.CameraModel"""
    def getContinuous(self) -> bool:
        """getContinuous(self: depthai.beta.node.Stitching) -> bool"""
    def getEstimationFrames(self) -> int:
        """getEstimationFrames(self: depthai.beta.node.Stitching) -> int"""
    def getMaxRange(self, unit: depthai.LengthUnit = ...) -> float:
        """getMaxRange(self: depthai.beta.node.Stitching, unit: depthai.LengthUnit = <LengthUnit.CENTIMETER: 1>) -> float"""
    def getMinIncidenceAngle(self) -> float:
        """getMinIncidenceAngle(self: depthai.beta.node.Stitching) -> float"""
    def getMode(self) -> Stitching.Mode:
        """getMode(self: depthai.beta.node.Stitching) -> depthai.beta.node.Stitching.Mode"""
    def getNumInputs(self) -> int:
        """getNumInputs(self: depthai.beta.node.Stitching) -> int

        Number of inputs the node was built with.
        """
    def getPanoConfidenceThreshold(self) -> float:
        """getPanoConfidenceThreshold(self: depthai.beta.node.Stitching) -> float"""
    def getPlane(self) -> Stitching.Plane | None:
        """getPlane(self: depthai.beta.node.Stitching) -> Optional[depthai.beta.node.Stitching.Plane]"""
    def getSeamFinder(self) -> Stitching.SeamFinder:
        """getSeamFinder(self: depthai.beta.node.Stitching) -> depthai.beta.node.Stitching.SeamFinder"""
    def getView(self) -> Stitching.VirtualCamera | None:
        """getView(self: depthai.beta.node.Stitching) -> Optional[depthai.beta.node.Stitching.VirtualCamera]"""
    def resetTransform(self) -> None:
        """resetTransform(self: depthai.beta.node.Stitching) -> None

        Discard the fixed transform and composition state and re-run the estimation. In
        `Mode::PLANAR_PROJECTION` the projection maps, seams and exposure gains are
        rebuilt from the next synced group. In non-continuous `Mode::PANORAMA`,
        registration is repeated and the fixed maps, regions, seams and exposure
        parameters are rebuilt.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.Stitching) -> bool

        Check whether the node is configured to run on host.
        """
    def setCameraModel(self, model: Stitching.CameraModel) -> None:
        """setCameraModel(self: depthai.beta.node.Stitching, model: depthai.beta.node.Stitching.CameraModel) -> None

        Set the projection surface the images are warped onto. Defaults to SPHERICAL,
        same as OpenCV. Only used in `Mode::PANORAMA`.
        """
    def setContinuous(self, continuous: bool) -> None:
        """setContinuous(self: depthai.beta.node.Stitching, continuous: bool) -> None

        Re-estimate the camera parameters on every frame. Only used in `Mode::PANORAMA`.

        When true, registration runs for every synced group, which is slow but tolerates
        cameras that move relative to each other. When false, registration evaluates
        getEstimationFrames() complete candidates without composing them, selects the
        one with the strongest geometrically consistent feature-match score, and then
        starts emitting panoramas using that transform. Projection maps, output regions,
        seam masks and exposure parameters are prepared with the first emitted panorama
        and reused for subsequent groups.
        """
    def setEstimationFrames(self, frames: int) -> None:
        """setEstimationFrames(self: depthai.beta.node.Stitching, frames: int) -> None

        Number of complete registration candidates evaluated before the strongest
        transform is fixed. Failed registrations, oversized candidates, and candidates
        that omit an input do not count. No panorama is emitted while the candidates are
        being evaluated. Only used when continuous is false.
        """
    def setMaxPanoramaSize(self, width: int, height: int) -> None:
        """setMaxPanoramaSize(self: depthai.beta.node.Stitching, width: int, height: int) -> None

        Reject panorama registrations whose projected canvas exceeds this size before
        OpenCV allocates and composes it. This protects against degenerate feature
        matches producing extremely large canvases. By default the size is unbounded.
        """
    def setMaxRange(self, range: float, unit: depthai.LengthUnit = ...) -> None:
        """setMaxRange(self: depthai.beta.node.Stitching, range: float, unit: depthai.LengthUnit = <LengthUnit.CENTIMETER: 1>) -> None

        Distance from a camera center beyond which the plane is not painted anymore.
        Bounds the automatic view and cuts off the region around the horizon, where a
        few pixels are stretched over a large part of the plane. Defaults to 10 meters.
        """
    def setMaxViewSize(self, width: int, height: int) -> None:
        """setMaxViewSize(self: depthai.beta.node.Stitching, width: int, height: int) -> None

        Upper bound on the size of the automatically computed view, in pixels. Defaults
        to 1920x1920.
        """
    def setMinIncidenceAngle(self, degrees: float) -> None:
        """setMinIncidenceAngle(self: depthai.beta.node.Stitching, degrees: float) -> None

        Smallest angle between a camera ray and the plane for the ray to still be used.
        Rays hitting the plane at a shallower angle are heavily stretched, so they are
        dropped. Defaults to 5 degrees.
        """
    def setMode(self, mode: Stitching.Mode) -> None:
        """setMode(self: depthai.beta.node.Stitching, mode: depthai.beta.node.Stitching.Mode) -> None

        Set the stitching mode. `Mode::PLANAR_PROJECTION` additionally needs a plane,
        see `setPlane()`.
        """
    def setPanoConfidenceThreshold(self, threshold: float) -> None:
        """setPanoConfidenceThreshold(self: depthai.beta.node.Stitching, threshold: float) -> None

        Confidence below which an image is dropped from the panorama
        """
    def setPlane(self, plane: Stitching.Plane) -> None:
        """setPlane(*args, **kwargs)
        Overloaded function.

        1. setPlane(self: depthai.beta.node.Stitching, plane: depthai.beta.node.Stitching.Plane) -> None

        Plane the images are projected onto, in `Mode::PLANAR_PROJECTION`. Required in
        that mode.

        The plane is expressed in the common origin frame of the incoming
        transformations, which is the camera socket their extrinsics are relative to. A
        plane that all cameras look at from the same side gives the usual bird's-eye
        view; the plane does not have to be horizontal.

        2. setPlane(self: depthai.beta.node.Stitching, point: depthai.Point3f, normal: depthai.Point3f, unit: depthai.LengthUnit = <LengthUnit.CENTIMETER: 1>) -> None
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.Stitching, runOnHost: bool) -> None

        Specify whether to run on host or an RVC4 device. By default, the node runs on
        host.
        """
    def setSeamFinder(self, finder: Stitching.SeamFinder) -> None:
        """setSeamFinder(self: depthai.beta.node.Stitching, finder: depthai.beta.node.Stitching.SeamFinder) -> None"""
    def setSyncThreshold(self, syncThreshold: datetime.timedelta) -> None:
        """setSyncThreshold(self: depthai.beta.node.Stitching, syncThreshold: datetime.timedelta) -> None

        Set the maximal interval between messages of a synced group.

        Parameter ``syncThreshold``:
            Maximal interval between messages in the group
        """
    def setView(self, view: Stitching.VirtualCamera) -> None:
        """setView(self: depthai.beta.node.Stitching, view: depthai.beta.node.Stitching.VirtualCamera) -> None

        Camera the plane is rendered from, in `Mode::PLANAR_PROJECTION`.

        By default the view is computed from the content: the node intersects the field
        of view of every input with the plane and places a camera looking straight at
        the plane so that all of the footprints fit, at a resolution derived from the
        inputs and bounded by `setMaxViewSize()`.
        """
    def setViewAuto(self) -> None:
        """setViewAuto(self: depthai.beta.node.Stitching) -> None"""
    @property
    def inputs(self) -> depthai.Node.InputMap:
        """A map of inputs, one per stitched source. Populated by build().
        (arg0: depthai.beta.node.Stitching) -> depthai.Node.InputMap
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Stitched image, ImgFrame of type BGR888i.
        (self: depthai.beta.node.Stitching) -> depthai.Node.Output
        """
    @property
    def sync(self):
        """Internal Sync node time-aligning the inputs.
        (self: depthai.beta.node.Stitching) -> dai::Subnode<dai::node::Sync>
        """

class SuperAnimalParser(depthai.DeviceNode):
    """SuperAnimalParser node. Parses the heatmap output of the SuperAnimal landmark
    neural network into a dai::beta::Keypoints message.

    The parser consumes a single output tensor. When the incoming NNData contains
    exactly one tensor, it is selected automatically; otherwise the output layer
    name must be configured explicitly or through an NNArchive head. The tensor is
    read in its stored order and must be a 4D tensor of shape (batch, height, width,
    numKeypoints) with a batch size of exactly 1. The number of keypoints and the
    heatmap size are derived from the tensor shape; the configured number of
    keypoints is informational only and does not affect the decoding. Per keypoint,
    the keypoint is the position of the maximum heatmap value with a 0.5-pixel
    center offset, mapped to input-image pixels and normalized by the configured
    scale factor without clipping, and the keypoint's score is the heatmap value at
    that position, which must lie in [0, 1]. Keypoints with a score strictly below
    the score threshold are dropped and the skeleton edges are remapped to the kept
    keypoints."""
    Properties: ClassVar[type[depthai.beta.SuperAnimalParserProperties]] = ...
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> SuperAnimalParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.SuperAnimalParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.SuperAnimalParser

        Build SuperAnimalParser node. Links the supplied output to this node's input and
        configures the parser from the model's SuperAnimalParser head.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.SuperAnimalParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.SuperAnimalParser

        Build SuperAnimalParser node with the specific head from an NNArchive. Useful
        when the model has multiple heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser
        """
    def getEdges(self, *args, **kwargs):
        """getEdges(self: depthai.beta.node.SuperAnimalParser) -> list[Annotated[list[int], FixedSize(2)]]

        Returns the skeleton edges as pairs of keypoint indices.
        """
    def getLabelNames(self) -> list[str]:
        """getLabelNames(self: depthai.beta.node.SuperAnimalParser) -> list[str]

        Returns the label names for the keypoints.
        """
    def getNumKeypoints(self) -> int:
        """getNumKeypoints(self: depthai.beta.node.SuperAnimalParser) -> int

        Returns the number of keypoints the model detects.
        """
    def getOutputLayerName(self) -> str:
        """getOutputLayerName(self: depthai.beta.node.SuperAnimalParser) -> str

        Returns the name of the model output layer to parse.
        """
    def getScaleFactor(self) -> float:
        """getScaleFactor(self: depthai.beta.node.SuperAnimalParser) -> float

        Returns the scale factor the keypoint coordinates are scaled and normalized by.
        """
    def getScoreThreshold(self) -> float:
        """getScoreThreshold(self: depthai.beta.node.SuperAnimalParser) -> float

        Returns the confidence score threshold for detected keypoints.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.SuperAnimalParser) -> bool

        Check if the node is set to run on host.
        """
    def setEdges(self, edges) -> None:
        """setEdges(self: depthai.beta.node.SuperAnimalParser, edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        Sets the skeleton edges as pairs of keypoint indices used for visualizing the
        skeleton.

        Example: {{0, 1}, {1, 2}, {2, 3}, {3, 0}} connects keypoint 0 to keypoint 1,
        keypoint 1 to keypoint 2, etc.

        Parameter ``edges``:
            Vector of keypoint index pairs
        """
    def setLabelNames(self, labelNames: list[str]) -> None:
        """setLabelNames(self: depthai.beta.node.SuperAnimalParser, labelNames: list[str]) -> None

        Sets the label names for the keypoints, indexed by keypoint index.

        Parameter ``labelNames``:
            Vector of label names
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.SuperAnimalParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one
        SuperAnimalParser head; use setNNArchiveHead() to select a specific head from a
        multi-head archive.

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        """setNNArchiveHead(self: depthai.beta.node.SuperAnimalParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head must be a SuperAnimalParser head with
        exactly one output layer.

        Parameter ``head:``:
            NNArchive head to set
        """
    def setNumKeypoints(self, nKeypoints: int) -> None:
        """setNumKeypoints(self: depthai.beta.node.SuperAnimalParser, nKeypoints: int) -> None

        Sets the number of keypoints the model detects.

        Informational only: the decoding derives the number of keypoints from the
        heatmap tensor's last dimension.

        Parameter ``nKeypoints``:
            Number of keypoints, must be greater than 0
        """
    def setOutputLayerName(self, outputLayerName: str) -> None:
        """setOutputLayerName(self: depthai.beta.node.SuperAnimalParser, outputLayerName: str) -> None

        Sets the name of the model output layer to parse.

        When left empty, the parser selects the tensor automatically if the incoming
        NNData contains exactly one tensor and fails otherwise.

        Parameter ``outputLayerName``:
            Name of the output layer
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.SuperAnimalParser, runOnHost: bool) -> None

        Specify whether to run on host or device. By default, the node runs on the
        device.
        """
    def setScaleFactor(self, scaleFactor: float) -> None:
        """setScaleFactor(self: depthai.beta.node.SuperAnimalParser, scaleFactor: float) -> None

        Sets the scale factor the keypoint coordinates are scaled and normalized by,
        typically the model input size.

        Parameter ``scaleFactor``:
            Scale factor, must be greater than 0
        """
    def setScoreThreshold(self, threshold: float) -> None:
        """setScoreThreshold(self: depthai.beta.node.SuperAnimalParser, threshold: float) -> None

        Sets the confidence score threshold for detected keypoints. Keypoints with a
        score strictly below the threshold are dropped.

        Parameter ``threshold``:
            Confidence score threshold, must be between 0 and 1 @note Configures startup
            behavior. Send SuperAnimalParserConfig to inputConfig after the pipeline
            starts.
        """
    @property
    def initialConfig(self) -> depthai.beta.SuperAnimalParserConfig:
        """Configuration used until a message is received on inputConfig.
        (self: depthai.beta.node.SuperAnimalParser) -> depthai.beta.SuperAnimalParserConfig
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with heatmaps data to parse.
        (self: depthai.beta.node.SuperAnimalParser) -> depthai.Node.Input
        """
    @property
    def inputConfig(self) -> depthai.Node.Input:
        """Runtime parser configuration. When synchronized, one configuration is consumed
        per frame; otherwise all queued configurations are drained and the newest valid
        one is used.
        (self: depthai.beta.node.SuperAnimalParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs Keypoints message with the detected animal keypoints.
        (self: depthai.beta.node.SuperAnimalParser) -> depthai.Node.Output
        """

class ToFStereoFusion(depthai.DeviceNode):
    """Experimental node that fuses aligned ToF and neural stereo-depth measurements.

    The node configures its internal ToF, neural-depth, alignment, synchronization,
    and fusion-network stages when built from a left and right camera.

    @note This node is supported on RVC4 devices only. Creating it for an RVC2
    device throws an exception."""
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, left: depthai.node.Camera, right: depthai.node.Camera) -> ToFStereoFusion:
        """build(self: depthai.beta.node.ToFStereoFusion, left: depthai.node.Camera, right: depthai.node.Camera) -> depthai.beta.node.ToFStereoFusion

        Configures fusion from a synchronized left and right camera pair.

        @note This node is supported on RVC4 devices only.

        Parameter ``left``:
            Left camera node.

        Parameter ``right``:
            Right camera node.

        Returns:
            This node.
        """
    @property
    def depth(self) -> depthai.Node.Output:
        """Fused depth output, aligned to the ToF sensor.
        (arg0: depthai.beta.node.ToFStereoFusion) -> depthai.Node.Output
        """
    @property
    def initialConfig(self) -> depthai.ToFStereoFusionConfig:
        """(self: depthai.beta.node.ToFStereoFusion) -> depthai.ToFStereoFusionConfig"""
    @property
    def inputLeft(self) -> depthai.Node.Input:
        """Left camera input used when the node runs outside the RVC4 device build.
        (arg0: depthai.beta.node.ToFStereoFusion) -> depthai.Node.Input
        """
    @property
    def inputRight(self) -> depthai.Node.Input:
        """Right camera input used when the node runs outside the RVC4 device build.
        (arg0: depthai.beta.node.ToFStereoFusion) -> depthai.Node.Input
        """
    @property
    def neuralConfidence(self) -> depthai.Node.Output:
        """Confidence output produced by the ToF-neural fusion network.
        (arg0: depthai.beta.node.ToFStereoFusion) -> depthai.Node.Output
        """
    @property
    def neuralDepth(self) -> depthai.node.NeuralDepth:
        """(arg0: depthai.beta.node.ToFStereoFusion) -> depthai.node.NeuralDepth"""
    @property
    def neuralNetwork(self) -> depthai.node.NeuralNetwork:
        """(arg0: depthai.beta.node.ToFStereoFusion) -> depthai.node.NeuralNetwork"""
    @property
    def tof(self) -> depthai.node.ToF:
        """(arg0: depthai.beta.node.ToFStereoFusion) -> depthai.node.ToF"""

class XFeatMonoParser(depthai.DeviceNode):
    '''XFeatMonoParser node. Parses the output of the XFeat model from one source (e.g.
    one camera) into a dai::TrackedFeatures message with the keypoints of a
    reference frame matched to the keypoints of the current frame.

    The parser consumes three output tensors, assigned from an NNArchive head by
    name substring or configured explicitly: the feature map (layer name containing
    "feats"), read in NCHW orientation as (1, descriptor size, height, width), the
    keypoint logits (name containing "keypoints"), read as (1, channels >= 64,
    height, width), and the reliability heat map (name containing "heatmaps"), read
    as (1, 1, height, width). Every frame is decoded into keypoints, scores and
    descriptors; the strongest keypoints (up to the maximum count) with a positive
    score are kept and their positions are scaled from the model input size to the
    original image size.

    The parser keeps a reference frame state: calling setTrigger() stores the next
    decoded result as the reference after that frame\'s message is emitted. Frames
    decoded while no reference is stored produce an empty TrackedFeatures message;
    afterwards each frame\'s keypoints are matched to the reference by mutual
    nearest-neighbor cosine similarity and emitted as feature pairs, where match i
    produces the reference position with id i and age 0 followed by the matched
    current position with id i and age 1. A frame whose keypoint heat map has no
    candidate produces an empty message and leaves the reference and the pending
    trigger untouched.

    The original image size must be configured before the pipeline starts, either
    through head metadata or with setOriginalSize().'''
    Properties: ClassVar[type[depthai.beta.XFeatMonoParserProperties]] = ...
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> XFeatMonoParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.XFeatMonoParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.XFeatMonoParser

        Build XFeatMonoParser node. Links the supplied output to this node's input and
        configures the parser from the model's XFeatMonoParser head; when the head
        metadata carries no input size, the input size is derived from the model input's
        declared shape and layout.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.XFeatMonoParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.XFeatMonoParser

        Build XFeatMonoParser node with the specific head from an NNArchive. Useful when
        the model has multiple heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser
        """
    def getInputSize(self) -> tuple[int, int]:
        """getInputSize(self: depthai.beta.node.XFeatMonoParser) -> tuple[int, int]

        Returns the model input image size as (width, height).
        """
    def getMaxKeypoints(self) -> int:
        """getMaxKeypoints(self: depthai.beta.node.XFeatMonoParser) -> int

        Returns the maximum number of keypoints to keep per frame.
        """
    def getOriginalSize(self) -> tuple[int, int] | None:
        """getOriginalSize(self: depthai.beta.node.XFeatMonoParser) -> Optional[tuple[int, int]]

        Returns the original image size as (width, height), or std::nullopt when not
        configured.
        """
    def getOutputLayerFeats(self) -> str:
        """getOutputLayerFeats(self: depthai.beta.node.XFeatMonoParser) -> str

        Returns the name of the output layer containing the feature map.
        """
    def getOutputLayerHeatmaps(self) -> str:
        """getOutputLayerHeatmaps(self: depthai.beta.node.XFeatMonoParser) -> str

        Returns the name of the output layer containing the reliability heat map.
        """
    def getOutputLayerKeypoints(self) -> str:
        """getOutputLayerKeypoints(self: depthai.beta.node.XFeatMonoParser) -> str

        Returns the name of the output layer containing the keypoint logits.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.XFeatMonoParser) -> bool

        Check if the node is set to run on host.
        """
    def setInputSize(self, width: int, height: int) -> None:
        """setInputSize(self: depthai.beta.node.XFeatMonoParser, width: int, height: int) -> None

        Sets the model input image size the keypoint positions are decoded in.

        Defaults to 640x352 like the source parser. Configuring from a full NNArchive
        takes it from the head metadata or derives it from the model input's declared
        shape and layout; the most recent configuration wins.

        Parameter ``width``:
            Input image width, must be greater than 0

        Parameter ``height``:
            Input image height, must be greater than 0
        """
    def setMaxKeypoints(self, maxKeypoints: int) -> None:
        """setMaxKeypoints(self: depthai.beta.node.XFeatMonoParser, maxKeypoints: int) -> None

        Sets the maximum number of keypoints to keep per frame.

        Parameter ``maxKeypoints``:
            Maximum number of keypoints @note Configures startup behavior. Send
            XFeatMonoParserConfig to inputConfig after the pipeline starts.
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.XFeatMonoParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one
        XFeatMonoParser head; use setNNArchiveHead() to select a specific head from a
        multi-head archive. When the head metadata carries no input size, the input size
        is derived from the model input's declared shape and layout (NHWC or NCHW).

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        '''setNNArchiveHead(self: depthai.beta.node.XFeatMonoParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head must be an XFeatMonoParser head with
        exactly three output layers; the layer whose name contains "feats" is used as
        the feature map layer, the layer whose name contains "keypoints" as the keypoint
        logit layer and the layer whose name contains "heatmaps" as the reliability heat
        map layer. The original size, input size and maximum keypoint count are read
        from the head metadata keys original_size, input_size and max_keypoints; missing
        keys keep the current values.

        Parameter ``head:``:
            NNArchive head to set
        '''
    def setOriginalSize(self, width: int, height: int) -> None:
        """setOriginalSize(self: depthai.beta.node.XFeatMonoParser, width: int, height: int) -> None

        Sets the original image size the emitted keypoint positions are scaled to.

        Must be configured before the pipeline starts, either explicitly or through head
        metadata.

        Parameter ``width``:
            Original image width, must be greater than 0

        Parameter ``height``:
            Original image height, must be greater than 0
        """
    def setOutputLayerFeats(self, outputLayerFeats: str) -> None:
        """setOutputLayerFeats(self: depthai.beta.node.XFeatMonoParser, outputLayerFeats: str) -> None

        Sets the name of the output layer containing the feature map.

        Parameter ``outputLayerFeats``:
            Name of the output layer containing the feature map
        """
    def setOutputLayerHeatmaps(self, outputLayerHeatmaps: str) -> None:
        """setOutputLayerHeatmaps(self: depthai.beta.node.XFeatMonoParser, outputLayerHeatmaps: str) -> None

        Sets the name of the output layer containing the reliability heat map.

        Parameter ``outputLayerHeatmaps``:
            Name of the output layer containing the reliability heat map
        """
    def setOutputLayerKeypoints(self, outputLayerKeypoints: str) -> None:
        """setOutputLayerKeypoints(self: depthai.beta.node.XFeatMonoParser, outputLayerKeypoints: str) -> None

        Sets the name of the output layer containing the keypoint logits.

        Parameter ``outputLayerKeypoints``:
            Name of the output layer containing the keypoint logits
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.XFeatMonoParser, runOnHost: bool) -> None

        Specify whether to run on host or device. By default, the node runs on the
        device.
        """
    def setTrigger(self) -> None:
        """setTrigger(self: depthai.beta.node.XFeatMonoParser) -> None

        Requests the reference frame update: after the next decoded frame's message is
        emitted, that frame's keypoints become the reference the following frames are
        matched against. May be called at any time while the pipeline runs.
        """
    @property
    def initialConfig(self) -> depthai.beta.XFeatMonoParserConfig:
        """Configuration used until a message is received on inputConfig.
        (self: depthai.beta.node.XFeatMonoParser) -> depthai.beta.XFeatMonoParserConfig
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with XFeat data to parse.
        (self: depthai.beta.node.XFeatMonoParser) -> depthai.Node.Input
        """
    @property
    def inputConfig(self) -> depthai.Node.Input:
        """Runtime parser configuration. When synchronized, one configuration is consumed
        per frame; otherwise all queued configurations are drained and the newest valid
        one is used.
        (self: depthai.beta.node.XFeatMonoParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs TrackedFeatures message with the matched keypoint pairs.
        (self: depthai.beta.node.XFeatMonoParser) -> depthai.Node.Output
        """

class XFeatStereoParser(depthai.DeviceNode):
    '''XFeatStereoParser node. Parses the output of the XFeat model from two sources
    (e.g. two cameras - left and right) into a dai::TrackedFeatures message with the
    keypoints of the reference frame matched to the keypoints of the target frame.

    The parser consumes three output tensors per source, assigned from an NNArchive
    head by name substring or configured explicitly: the feature map (layer name
    containing "feats"), read in NCHW orientation as (1, descriptor size, height,
    width), the keypoint logits (name containing "keypoints"), read as (1, channels
    >= 64, height, width), and the reliability heat map (name containing
    "heatmaps"), read as (1, 1, height, width). Both sources share one layer, size
    and keypoint-count configuration.

    Each iteration consumes one reference message followed by one target message;
    the node performs no synchronization beyond these two sequential blocking reads.
    Both messages are decoded into keypoints, scores and descriptors; the strongest
    keypoints (up to the maximum count) with a positive score are kept and their
    positions are scaled from the model input size to the original image size. When
    the reference (checked first) or the target frame\'s keypoint heat map has no
    candidate, an empty TrackedFeatures message is emitted carrying that frame\'s
    timestamps and the reference frame\'s sequence number. Otherwise the reference
    keypoints are matched to the target keypoints by mutual nearest-neighbor cosine
    similarity and emitted as feature pairs, where match i produces the reference
    position with id i and age 0 followed by the matched target position with id i
    and age 1; the message carries the target frame\'s timestamps and the reference
    frame\'s sequence number, like the source parser.

    The original image size must be configured before the pipeline starts, either
    through head metadata or with setOriginalSize().'''
    Properties: ClassVar[type[depthai.beta.XFeatStereoParserProperties]] = ...
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, reference: depthai.Node.Output, target: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> XFeatStereoParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.XFeatStereoParser, reference: depthai.Node.Output, target: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.XFeatStereoParser

        Build XFeatStereoParser node. Links the supplied reference output to this node's
        referenceInput and the supplied target output to this node's targetInput, and
        configures the parser from the model's XFeatStereoParser head; when the head
        metadata carries no input size, the input size is derived from the model input's
        declared shape and layout.

        Parameter ``reference:``:
            Output with the reference source's NN results to link to referenceInput

        Parameter ``target:``:
            Output with the target source's NN results to link to targetInput

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.XFeatStereoParser, reference: depthai.Node.Output, target: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.XFeatStereoParser

        Build XFeatStereoParser node with the specific head from an NNArchive. Useful
        when the model has multiple heads. Links the supplied reference output to this
        node's referenceInput and the supplied target output to this node's targetInput.

        Parameter ``reference:``:
            Output with the reference source's NN results to link to referenceInput

        Parameter ``target:``:
            Output with the target source's NN results to link to targetInput

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser
        """
    def getInputSize(self) -> tuple[int, int]:
        """getInputSize(self: depthai.beta.node.XFeatStereoParser) -> tuple[int, int]

        Returns the model input image size as (width, height).
        """
    def getMaxKeypoints(self) -> int:
        """getMaxKeypoints(self: depthai.beta.node.XFeatStereoParser) -> int

        Returns the maximum number of keypoints to keep per frame.
        """
    def getOriginalSize(self) -> tuple[int, int] | None:
        """getOriginalSize(self: depthai.beta.node.XFeatStereoParser) -> Optional[tuple[int, int]]

        Returns the original image size as (width, height), or std::nullopt when not
        configured.
        """
    def getOutputLayerFeats(self) -> str:
        """getOutputLayerFeats(self: depthai.beta.node.XFeatStereoParser) -> str

        Returns the name of the output layer containing the feature map.
        """
    def getOutputLayerHeatmaps(self) -> str:
        """getOutputLayerHeatmaps(self: depthai.beta.node.XFeatStereoParser) -> str

        Returns the name of the output layer containing the reliability heat map.
        """
    def getOutputLayerKeypoints(self) -> str:
        """getOutputLayerKeypoints(self: depthai.beta.node.XFeatStereoParser) -> str

        Returns the name of the output layer containing the keypoint logits.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.XFeatStereoParser) -> bool

        Check if the node is set to run on host.
        """
    def setInputSize(self, width: int, height: int) -> None:
        """setInputSize(self: depthai.beta.node.XFeatStereoParser, width: int, height: int) -> None

        Sets the model input image size the keypoint positions are decoded in.

        Defaults to 640x352 like the source parser. Configuring from a full NNArchive
        takes it from the head metadata or derives it from the model input's declared
        shape and layout; the most recent configuration wins.

        Parameter ``width``:
            Input image width, must be greater than 0

        Parameter ``height``:
            Input image height, must be greater than 0
        """
    def setMaxKeypoints(self, maxKeypoints: int) -> None:
        """setMaxKeypoints(self: depthai.beta.node.XFeatStereoParser, maxKeypoints: int) -> None

        Sets the maximum number of keypoints to keep per frame.

        Parameter ``maxKeypoints``:
            Maximum number of keypoints @note Configures startup behavior. Send
            XFeatStereoParserConfig to inputConfig after the pipeline starts.
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.XFeatStereoParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one
        XFeatStereoParser head; use setNNArchiveHead() to select a specific head from a
        multi-head archive. When the head metadata carries no input size, the input size
        is derived from the model input's declared shape and layout (NHWC or NCHW).

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        '''setNNArchiveHead(self: depthai.beta.node.XFeatStereoParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head must be an XFeatStereoParser head
        with exactly three output layers; the layer whose name contains "feats" is used
        as the feature map layer, the layer whose name contains "keypoints" as the
        keypoint logit layer and the layer whose name contains "heatmaps" as the
        reliability heat map layer. The original size, input size and maximum keypoint
        count are read from the head metadata keys original_size, input_size and
        max_keypoints; missing keys keep the current values.

        Parameter ``head:``:
            NNArchive head to set
        '''
    def setOriginalSize(self, width: int, height: int) -> None:
        """setOriginalSize(self: depthai.beta.node.XFeatStereoParser, width: int, height: int) -> None

        Sets the original image size the emitted keypoint positions are scaled to.

        Must be configured before the pipeline starts, either explicitly or through head
        metadata.

        Parameter ``width``:
            Original image width, must be greater than 0

        Parameter ``height``:
            Original image height, must be greater than 0
        """
    def setOutputLayerFeats(self, outputLayerFeats: str) -> None:
        """setOutputLayerFeats(self: depthai.beta.node.XFeatStereoParser, outputLayerFeats: str) -> None

        Sets the name of the output layer containing the feature map.

        Parameter ``outputLayerFeats``:
            Name of the output layer containing the feature map
        """
    def setOutputLayerHeatmaps(self, outputLayerHeatmaps: str) -> None:
        """setOutputLayerHeatmaps(self: depthai.beta.node.XFeatStereoParser, outputLayerHeatmaps: str) -> None

        Sets the name of the output layer containing the reliability heat map.

        Parameter ``outputLayerHeatmaps``:
            Name of the output layer containing the reliability heat map
        """
    def setOutputLayerKeypoints(self, outputLayerKeypoints: str) -> None:
        """setOutputLayerKeypoints(self: depthai.beta.node.XFeatStereoParser, outputLayerKeypoints: str) -> None

        Sets the name of the output layer containing the keypoint logits.

        Parameter ``outputLayerKeypoints``:
            Name of the output layer containing the keypoint logits
        """
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.XFeatStereoParser, runOnHost: bool) -> None

        Specify whether to run on host or device. By default, the node runs on the
        device.
        """
    @property
    def initialConfig(self) -> depthai.beta.XFeatStereoParserConfig:
        """Configuration used until a message is received on inputConfig.
        (self: depthai.beta.node.XFeatStereoParser) -> depthai.beta.XFeatStereoParserConfig
        """
    @property
    def inputConfig(self) -> depthai.Node.Input:
        """Runtime parser configuration. When synchronized, one configuration is consumed
        per frame pair; otherwise all queued configurations are drained and the newest
        valid one is used.
        (self: depthai.beta.node.XFeatStereoParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs TrackedFeatures message with the matched keypoint pairs.
        (self: depthai.beta.node.XFeatStereoParser) -> depthai.Node.Output
        """
    @property
    def referenceInput(self) -> depthai.Node.Input:
        """Input NN results of the reference source (e.g. the left camera) with XFeat data
        to parse.
        (self: depthai.beta.node.XFeatStereoParser) -> depthai.Node.Input
        """
    @property
    def targetInput(self) -> depthai.Node.Input:
        """Input NN results of the target source (e.g. the right camera) with XFeat data to
        parse.
        (self: depthai.beta.node.XFeatStereoParser) -> depthai.Node.Input
        """

class YuNetParser(depthai.DeviceNode):
    '''YuNetParser node. Parses the output of the YuNet face detection model into a
    dai::ImgDetections message containing the bounding boxes, labels, confidence
    scores and 5 facial keypoints per detected face, everything normalized to [0,
    1]. The decoding is based on https://github.com/Kazuhito00/YuNet-ONNX-TFLite-
    Sample (Apache License 2.0).

    The parser consumes three output tensors: a loc tensor with 14 values per anchor
    (2 bounding box center offsets, 2 bounding box size values and 5 keypoint
    coordinate offset pairs), a conf tensor with 2 values per anchor (non-face and
    face scores) and an iou tensor with 1 value per anchor. When a layer name is not
    configured, it is auto-detected from the incoming NNData as the single layer
    name starting with "loc", "conf" or "iou" respectively. The candidate scores are
    sqrt(conf face score * iou score clipped to [0, 1]); candidates with a score
    strictly greater than the confidence threshold are decoded against the YuNet
    anchors generated from the input size, suppressed with cv2.dnn.NMSBoxes-
    semantics non-maximum suppression (IoU threshold, maximum number of detections
    as the top-k limit) and emitted in descending score order. Keypoint coordinates
    are truncated to whole pixels before normalization, mirroring the source parser.
    The anchors are cached across messages and refreshed when the input size
    changes.'''
    Properties: ClassVar[type[depthai.beta.YuNetParserProperties]] = ...
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def build(self, input: depthai.Node.Output, model: depthai.NNModelDescription | depthai.NNArchive | str) -> YuNetParser:
        """build(*args, **kwargs)
        Overloaded function.

        1. build(self: depthai.beta.node.YuNetParser, input: depthai.Node.Output, model: Union[depthai.NNModelDescription, depthai.NNArchive, str]) -> depthai.beta.node.YuNetParser

        Build YuNetParser node. Links the supplied output to this node's input and
        configures the parser from the model's YuNetParser head and the model input's
        declared shape and layout.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``model:``:
            Neural network model

        2. build(self: depthai.beta.node.YuNetParser, input: depthai.Node.Output, head: depthai.nn_archive.v1.Head) -> depthai.beta.node.YuNetParser

        Build YuNetParser node with the specific head from an NNArchive. Useful when the
        model has multiple heads.

        Parameter ``nnInput:``:
            Output to link

        Parameter ``head:``:
            Specific head from NNArchive to use for this parser @note A head carries no
            model input metadata, so the input size keeps its current value; configure
            it with setInputSize() when it is not set yet.
        """
    def getConfidenceThreshold(self) -> float:
        """getConfidenceThreshold(self: depthai.beta.node.YuNetParser) -> float

        Returns the confidence score threshold for detected faces.
        """
    def getInputSize(self) -> tuple[int, int] | None:
        """getInputSize(self: depthai.beta.node.YuNetParser) -> Optional[tuple[int, int]]

        Returns the model input image size as (width, height), or std::nullopt when it
        is not set.
        """
    def getIouThreshold(self) -> float:
        """getIouThreshold(self: depthai.beta.node.YuNetParser) -> float

        Returns the non-maximum suppression (IoU) threshold.
        """
    def getLabelNames(self) -> list[str]:
        """getLabelNames(self: depthai.beta.node.YuNetParser) -> list[str]

        Returns the label names for the detected faces.
        """
    def getMaxDetections(self) -> int:
        """getMaxDetections(self: depthai.beta.node.YuNetParser) -> int

        Returns the maximum number of detections to keep.
        """
    def getOutputLayerConf(self) -> str:
        """getOutputLayerConf(self: depthai.beta.node.YuNetParser) -> str

        Returns the name of the output layer containing the confidence predictions.
        """
    def getOutputLayerIou(self) -> str:
        """getOutputLayerIou(self: depthai.beta.node.YuNetParser) -> str

        Returns the name of the output layer containing the IoU predictions.
        """
    def getOutputLayerLoc(self) -> str:
        """getOutputLayerLoc(self: depthai.beta.node.YuNetParser) -> str

        Returns the name of the output layer containing the location predictions.
        """
    def runOnHost(self) -> bool:
        """runOnHost(self: depthai.beta.node.YuNetParser) -> bool

        Check if the node is set to run on host.
        """
    def setConfidenceThreshold(self, threshold: float) -> None:
        """setConfidenceThreshold(self: depthai.beta.node.YuNetParser, threshold: float) -> None

        Sets the confidence score threshold for detected faces. Detections with a score
        strictly greater than the threshold are kept.

        Parameter ``threshold``:
            Confidence score threshold @note Configures startup behavior. Send
            YuNetParserConfig to inputConfig after the pipeline starts.
        """
    def setInputSize(self, width: int, height: int) -> None:
        """setInputSize(self: depthai.beta.node.YuNetParser, width: int, height: int) -> None

        Sets the model input image size the anchors are computed against and the emitted
        coordinates are normalized by. Unset by default; it must be configured from an
        NNArchive or with this setter before the parser processes messages.

        Configuring from a full NNArchive derives it from the model input's declared
        shape and layout; the most recent configuration wins.

        Parameter ``width``:
            Input image width, must be greater than 0

        Parameter ``height``:
            Input image height, must be greater than 0
        """
    def setIouThreshold(self, threshold: float) -> None:
        """setIouThreshold(self: depthai.beta.node.YuNetParser, threshold: float) -> None

        Sets the non-maximum suppression (IoU) threshold. Candidates whose overlap with
        a kept detection is at most the threshold (inclusive) survive suppression.

        Parameter ``threshold``:
            Non-maximum suppression threshold @note Configures startup behavior. Send
            YuNetParserConfig to inputConfig after the pipeline starts.
        """
    def setLabelNames(self, labelNames: list[str]) -> None:
        '''setLabelNames(self: depthai.beta.node.YuNetParser, labelNames: list[str]) -> None

        Sets the label names for the detected faces. The first label name is assigned to
        every detection (all detections carry label 0). When empty, no label name is
        assigned. Defaults to ("Face").

        Parameter ``labelNames``:
            List of label names
        '''
    def setMaxDetections(self, maxDetections: int) -> None:
        """setMaxDetections(self: depthai.beta.node.YuNetParser, maxDetections: int) -> None

        Sets the maximum number of detections to keep, applied as the non-maximum
        suppression top-k limit (no limit when 0 or negative).

        Parameter ``maxDetections``:
            Maximum number of detections to keep @note Configures startup behavior. Send
            YuNetParserConfig to inputConfig after the pipeline starts.
        """
    def setNNArchive(self, nnArchive: depthai.NNArchive) -> None:
        """setNNArchive(self: depthai.beta.node.YuNetParser, nnArchive: depthai.NNArchive) -> None

        Set NNArchive for this Node. The archive must contain exactly one YuNetParser
        head and exactly one model input; use setNNArchiveHead() to select a specific
        head from a multi-head archive. The input size is derived from the model input's
        declared shape and layout (NHWC or NCHW).

        Parameter ``nnArchive:``:
            NNArchive to set
        """
    def setNNArchiveHead(self, head: depthai.nn_archive.v1.Head) -> None:
        '''setNNArchiveHead(self: depthai.beta.node.YuNetParser, head: depthai.nn_archive.v1.Head) -> None

        Set NNArchive head for this Node. The head must be a YuNetParser head; every
        head output layer name must contain "loc", "conf" or "iou" and is routed to the
        matching output layer name setting.

        Parameter ``head:``:
            NNArchive head to set @note A head carries no model input metadata, so the
            input size keeps its current value; configure it with setInputSize() when it
            is not set yet.
        '''
    def setOutputLayerConf(self, confOutputLayerName: str) -> None:
        '''setOutputLayerConf(self: depthai.beta.node.YuNetParser, confOutputLayerName: str) -> None

        Sets the name of the output layer containing the confidence predictions. When
        left empty, the name is auto-detected from the incoming NNData as the single
        layer name starting with "conf".

        Parameter ``confOutputLayerName``:
            Output layer name for the conf tensor
        '''
    def setOutputLayerIou(self, iouOutputLayerName: str) -> None:
        '''setOutputLayerIou(self: depthai.beta.node.YuNetParser, iouOutputLayerName: str) -> None

        Sets the name of the output layer containing the IoU predictions. When left
        empty, the name is auto-detected from the incoming NNData as the single layer
        name starting with "iou".

        Parameter ``iouOutputLayerName``:
            Output layer name for the IoU tensor
        '''
    def setOutputLayerLoc(self, locOutputLayerName: str) -> None:
        '''setOutputLayerLoc(self: depthai.beta.node.YuNetParser, locOutputLayerName: str) -> None

        Sets the name of the output layer containing the location predictions. When left
        empty, the name is auto-detected from the incoming NNData as the single layer
        name starting with "loc".

        Parameter ``locOutputLayerName``:
            Output layer name for the loc tensor
        '''
    def setRunOnHost(self, runOnHost: bool) -> None:
        """setRunOnHost(self: depthai.beta.node.YuNetParser, runOnHost: bool) -> None

        Specify whether to run on host or device. By default, the node runs on the
        device.
        """
    @property
    def initialConfig(self) -> depthai.beta.YuNetParserConfig:
        """Configuration used when the parser starts.
        (self: depthai.beta.node.YuNetParser) -> depthai.beta.YuNetParserConfig
        """
    @property
    def input(self) -> depthai.Node.Input:
        """Input NN results with YuNet detection data to parse.
        (self: depthai.beta.node.YuNetParser) -> depthai.Node.Input
        """
    @property
    def inputConfig(self) -> depthai.Node.Input:
        """Runtime parser configuration. In synchronized mode one configuration is consumed
        per input frame; otherwise all queued configurations are drained and the newest
        valid one is retained.
        (self: depthai.beta.node.YuNetParser) -> depthai.Node.Input
        """
    @property
    def out(self) -> depthai.Node.Output:
        """Outputs ImgDetections message with the bounding boxes, labels, confidence scores
        and keypoints of the detected faces.
        (self: depthai.beta.node.YuNetParser) -> depthai.Node.Output
        """
